---
updated: 2026-09-03
generated-by: .claude/supervision/scan_transcripts.py (superviseur d'agents, étage 1)
---

# Supervision des agents — tableau de bord d'usage

> ⚠️ **Page générée automatiquement** (hook SessionStart → `.claude/supervision/scan_transcripts.py`).
> **Ne pas éditer à la main** — toute modification serait écrasée au prochain scan.

Dernier scan : 2026-09-03T09:59:07+02:00 · **139 sessions** (transcripts) · **214** invocations de skills · **193** lancements de sous-agents.

## Skills — usage réel

| Skill | Famille | Invocations | Première | Dernière |
| --- | --- | --- | --- | --- |
| `agent-orchestrator` | projet | 120 | 2026-07-23 | 2026-09-03 |
| `agent-supervisor` | projet | 18 | 2026-07-23 | 2026-09-02 |
| `audit-technique` | projet | 12 | 2026-07-24 | 2026-09-02 |
| `bmad-review-edge-case-hunter` | BMAD | 12 | 2026-07-31 | 2026-09-02 |
| `revue-increment` | projet | 11 | 2026-07-29 | 2026-09-03 |
| `bmad-code-review` | BMAD | 9 | 2026-07-31 | 2026-09-02 |
| `bmad-party-mode` | BMAD | 8 | 2026-07-30 | 2026-09-02 |
| `bmad-review-adversarial-general` | BMAD | 7 | 2026-07-31 | 2026-09-02 |
| `veille-agentic` | projet | 6 | 2026-07-27 | 2026-09-03 |
| `bmad-editorial-review-structure` | BMAD | 2 | 2026-09-03 | 2026-09-03 |
| `dataviz` | (builtin/session) | 2 | 2026-07-31 | 2026-07-31 |
| `artifact-design` | (builtin/session) | 1 | 2026-08-31 | 2026-08-31 |
| `bmad-advanced-elicitation` | BMAD | 1 | 2026-09-02 | 2026-09-02 |
| `bmad-customize` | BMAD | 1 | 2026-07-31 | 2026-07-31 |
| `bmad-technical-research` | BMAD | 1 | 2026-07-30 | 2026-07-30 |
| `pdf-quality` | projet | 1 | 2026-08-31 | 2026-08-31 |
| `run` | (builtin/session) | 1 | 2026-07-29 | 2026-07-29 |
| `update-config` | (builtin/session) | 1 | 2026-07-24 | 2026-07-24 |

## Sous-agents

| Sous-agent | Lancements | Premier | Dernier |
| --- | --- | --- | --- |
| `general-purpose` | 126 | 2026-07-23 | 2026-09-02 |
| `Explore` | 35 | 2026-07-23 | 2026-09-02 |
| `bmad-revue` | 14 | 2026-07-31 | 2026-09-02 |
| `agent-supervisor` | 13 | 2026-07-30 | 2026-09-02 |
| `veille-agentic` | 4 | 2026-08-31 | 2026-09-03 |
| `bmad-recherche` | 1 | 2026-07-30 | 2026-07-30 |

## Jamais utilisés

**BMAD** — 38/46 jamais invoqués :

<details><summary>Voir la liste</summary>

`bmad-agent-analyst`, `bmad-agent-architect`, `bmad-agent-dev`, `bmad-agent-pm`, `bmad-agent-tech-writer`, `bmad-agent-ux-designer`, `bmad-architecture`, `bmad-brainstorming`, `bmad-check-implementation-readiness`, `bmad-checkpoint-preview`, `bmad-correct-course`, `bmad-create-architecture`, `bmad-create-epics-and-stories`, `bmad-create-prd`, `bmad-create-story`, `bmad-dev-auto`, `bmad-dev-story`, `bmad-document-project`, `bmad-domain-research`, `bmad-edit-prd`, `bmad-editorial-review-prose`, `bmad-forge-idea`, `bmad-generate-project-context`, `bmad-help`, `bmad-index-docs`, `bmad-market-research`, `bmad-prd`, `bmad-prfaq`, `bmad-product-brief`, `bmad-qa-generate-e2e-tests`, `bmad-quick-dev`, `bmad-retrospective`, `bmad-shard-doc`, `bmad-spec`, `bmad-sprint-planning`, `bmad-sprint-status`, `bmad-ux`, `bmad-validate-prd`

</details>

**global** — 1/2 jamais invoqués :

`skill-creator`

## Skills bibliothèque / référence

_Consommés en lisant/exécutant leurs `scripts/`, ou via un sous-agent qui les suit (ex. `ppt-designer`, qui n'a pas l'outil Skill) — le compteur d'invocations ne peut structurellement pas les voir. `n=0` n'y vaut donc PAS « mort » : ne pas désinstaller sur ce seul signal (constat superviseur #2)._

`deck-design-library`, `pptx-deck`, `pptx-framed-image`, `pptx-verify`, `restitution-deck-design`, `roadmap-keeper`, `slide-text-polish`

## TODO agents (constats automatiques)

1. **Élaguer les skills BMAD** : 38/46 jamais invoqués — confirmer l'utilité des non-utilisés.
2. **Skills en sommeil (>30 j sans usage)** : `bmad-customize`, `bmad-recherche`, `bmad-technical-research`, `dataviz`, `run`, `update-config`.

## Arbitrages enregistrés

_Constats clos par décision humaine (`.claude/supervision/arbitrages.json`) — l'usage réel reste mesuré ci-dessus._

- **`famille:linter`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« traite tout ») : ruff configuré (pyproject.toml, baseline F/I/UP/B) sur VSCode2 (102 points mesurés, B008 ignoré car idiome FastAPI) et VScode5 (0 point après nettoyage). PAS de --fix aveugle : un ruff --fix sur VSCode2 a retiré un ré-export (is_configured) et cassé un import — reverté, tests re-verts (40 passed) ; correction au fil de l'eau. VSCode/VSCode3/VSCode4 hors périmètre (peu de code).
- **`famille:revue-code`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« traite tout ») : hook warn_verif_before_commit porté de VSCode1 vers VSCode2, marqueurs adaptés (pytest/pptx-verify/revue-increment au lieu de npm test), câblé en PreToolUse. Rappel non bloquant au commit si du code app/ part sans vérif réelle dans la session. Fail-open vérifié. L'agent reviewer dédié reste optionnel (coût).
- **`famille:design-review`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« traite tout ») : skill deck-design-review greffée sur VSCode4, RÉÉCRITE pour son deck OHC réel (15 slides : couverture, sommaire, dividers chapitre teardrop, leviers, personas, architecture, existant, évaluation, mentorat, arbitrages, séquencement, roadmap) et son canal (rendu LibreOffice, générateur generate_deck_ohc.py) — pas un copier-coller de VSCode2. VSCode3 reste à traiter.
- **`famille:cadrage-produit`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« traite tout ») : product-brief rédigé pour VScode5 (docs/product-brief.md — persona = orchestrateur de flotte, why = ne pas re-découvrir/re-corriger le même écart projet par projet, besoins = voir/alerter/comparer/approfondir/corriger/veiller, proposition de valeur = piloter la flotte comme un seul système via la boucle propose→arbitre→applique). VSCode4 (deck RH) reste à cadrer sur demande. Autres projets : fragments suffisants.
- **`famille:documentation`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« applique la reco ») : le hub VScode5, jusque-là sans README ni CLAUDE.md, en est doté — README.md (ce que fait le dispositif, démarrage, skills, structure, boucle d'amélioration) et CLAUDE.md (règles R1-R5 pour agir sur la flotte : lire l'état réel, commit scopé, adapter au canal, propose→arbitre→applique, vérité du journal + vérifications avant commit). Dimension documentation VScode5 : moyen → ok. Non traité sur VSCode/VSCode3/VSCode4 (README via bmad-document-project sur demande) — hors périmètre de cet arbitrage.
- **`famille:tests`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« applique la reco pratique-test ») : couverture de test outillée sur les 2 projets à forte densité de test, via le playbook evolution-flotte. VSCode2 : pytest-cov ajouté à requirements-dev.txt, commande documentée dans conventions.md, 1ère mesure ~38 % (pptx_deck 56 %, pptx_export 48 %). VSCode1 : c8 en devDep + script npm test:cov + étape CI, 1ère mesure 84,67 % lignes. Aucun seuil imposé (on mesure d'abord). Non traité sur VSCode/VSCode3/VSCode4 (peu de code / pré-code) — hors périmètre assumé.
- **`scan_transcripts.py`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« traite tout ») : le scan compte désormais les skills invoquées en slash-command (<command-name> filtré sur les skills installées), le hint revue-increment est conditionnel à la présence réelle de la skill, et le fix slug (caractères non alphanumériques → tiret) est propagé. Recomptage complet de ce projet fait (agent-orchestrator ×3, agent-supervisor ×1 mesurés). Les 3 édits sont propagés aux 5 autres copies de la flotte par édits ciblés vérifiés (py_compile vert partout).
- **`log_run.py`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« traite tout ») : mode --solde <prefixe-ts> <resultat> "note" ajouté (requalification tracée d'un run en-attente-validation — la validation utilisateur devient un événement de première classe du journal, testée sur les chemins nominal et erreurs). Propagé aux 5 autres copies (copie directe sur les identiques, insertion ciblée sur les divergées). Le bandeau du wiki affiche la commande.
- **`playbooks`** (2026-07-23) : ACCEPTÉ + APPLIQUÉ (« traite tout ») : playbook evolution-flotte créé (cadrage sur l'état réel → modification scopée → vérifications → commit limité au périmètre → wiki → journal), enregistré au catalogue et dans la table des playbooks de l'orchestrateur, statut éprouvé (capitalisé des 4 runs flotte du 2026-07-23).
- **`famille:dispositif-partage`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ (P1 de l'analyse craft) : la dette risque_technique CRITIQUE (audit VScode5 — 6 copies divergentes de scan_transcripts.py/log_run.py maintenues à la main) est résorbée par un canon unique + synchronisation, remplaçant la propagation manuelle par édits ciblés du 2026-07-23 (l'approche qui avait laissé diverger les copies). Canon dans .claude/dispositif/canon/, propagé par sync_dispositif.py (en-tête « généré — ne pas éditer localement », modes --check/--projet). Le cadrage a révélé que la dette n'était pas uniforme : version canonique de fait partagée par 3 projets + 2 améliorations éparpillées jamais réunies — détection des skills consommées par lecture (ex-VSCode1) et arbitrage restreint par catégorie (ex-VSCode3). Le canon = UNION des deux : chaque projet GAGNE ce qui lui manquait, aucun ne perd. Commits scopés (2 fichiers/cible + dispositif/ au hub) et push main sur les 6 remotes ; scans relancés 6/6 sans échec. Suivi : re-lancer audit-technique VScode5 pour sortir la dimension de « critique ».
- **`canon-p1-regression`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur « garder A, fixer le test ») : l'enrichissement A du canon (non_invocation_skills — détection des skills consommées par lecture) a fait échouer 1 test de la suite VSCode2 (un exemple de test supposait qu'un skill sans dossier scripts/ était « jamais utilisé »). Décision : conserver l'amélioration A (comportement voulu) et corriger le test — l'exemple devient priority-matrix (skill sans scripts/ authentiquement non invoquée). Suite complète VSCode2 re-vérifiée VERTE (330 passed) avant tout commit aval. Illustre la valeur du canon partagé : la régression est apparue et a été traitée UNE fois, pas 6.
- **`risque-technique:VSCode1-scores`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ (P2 volet VSCode1) : le cœur de calcul (agrégation de scores / moyenne / écart-type), sans aucun test unitaire (finding audit VSCode1 risque_technique), est extrait dans scores.js et couvert par test-scores.js — npm test vert. R1 a évité de refaire la couverture (c8 déjà en place, 84,67 % — arbitrage famille:tests du 2026-07-23) : seul le trou réel (test du calcul) est comblé. Reste optionnel : brancher le test du chemin export dans npm test.
- **`famille:integration-continue`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ (P3) : CI GitHub Actions ajoutée à VSCode2 — seul projet Python avec tests + couverture + ruff mais sans CI (finding pratique-dev, capability DORA « intégration continue »). Workflow sur push main + PR : Python 3.12, install requirements-dev, ruff en NON bloquant (baseline assumée, « mesure d'abord »), pytest --cov=app comme gate. Modèle : la CI JS de VSCode1. Le volet linter de P3 (ruff sur VSCode/VSCode3/VSCode4) reste REFUSÉ/hors périmètre — déjà arbitré le 2026-07-23 (peu de code).
- **`famille:pratiques-providers`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ (« lance les chantiers 1 à 4 ») : les 4 pratiques du volet 2 de veille-agentic passent en adopte. (1) Hook de vérif pré-commit propagé à VSCode/VSCode3/VSCode4, adapté au canal de chacun (COMOP smoke-test / générateur pytest), commits scopés poussés ; en chemin, la même régression canon-P1 que VSCode2 détectée sur le test VSCode3 et corrigée selon l'arbitrage existant (exemple deck-design-library, 29 verts). (2) Règle scan « CLAUDE.md ≤ 150 lignes » (fonction pure + 7 tests) + élagage VSCode1 158→149 et VSCode3 285→107 (changelog historique du deck retiré, contraintes durables conservées). (3) Étape revue-fraiche (contexte frais, sonnet) ancrée dans evolution-flotte et export-ppt-verifie (JSON validés). (4) Hub aligné garde-fous en couches : 6 deny rules (secrets locaux + secrets des projets CIBLES qu'il lit), dimension sécurité 🟠→🟢.
- **`famille:dispositif-package`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ (demande « package de déploiement agentic ») : .claude/dispositif/package/deploy_nouveau_projet.py — bootstrap complet d'un nouveau projet SANS copies au repos (leçon P1) : manifeste de 21 sources vivantes (canon du hub, hooks hub/VSCode3, kit d'export VSCode2, tests VSCode3) matérialisées à la demande + settings.json câblé (4 hooks + deny rules) + squelette CLAUDE.md (< 150 l) + arbitrages.json vide. Modes --check (sources vivantes) et --force. Vérifié par déploiement d'essai réel : 25 fichiers, tout compile, JSON valides, 26/29 tests verts pré-BMAD (3 rouges attendus, documentés dans la checklist avec l'install BMAD). Leçon capturée : basetemp court obligatoire (MAX_PATH Windows).
- **`wiki-site-actions`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ (demandes « boutons déclencheurs + onglets + exports PDF ») : le wiki devient un site web — 5 onglets thématiques (Pilotage/Projets/Pratiques & risques/Veille/Actions & exports, hash persistant, nav sticky) ; onglet Actions avec déclencheurs 0-token (re-scan, sync --check, package --check, régénération PDF) et LLM via claude -p (diagnostic, audit par projet, veille, remédiation par finding — gouvernance propose→arbitre→applique préservée dans les prompts) ; serveur scripts/serve_wiki.py (127.0.0.1 UNIQUEMENT — leçon audit VSCode 0.0.0.0 —, allowlist stricte, suivi de jobs) testé bout en bout ; 2 exports PDF téléchargeables (analyse détaillée par projet avec findings localisés = exemples et synthèses = commentaires ; actions de remédiation avec propositions de veille et arbitrages appliqués commentés), régénérés via Edge headless, vérifiés visuellement.
- **`log_run.py`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ (lance les actions correctives) : run 09:43 (veille pratiques) soldé succès via log_run --solde ; cycle de 8 commits UI (13c0713..0733e03) journalisé rétroactivement en un run de synthèse (6 étapes, 2 reprises réelles tracées).
- **`audit-technique:VScode5`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ : re-audit VScode5 sur la surface serve_wiki.py. 3 findings sécurité RÉELS trouvés (XSS stocké via libelle/tail non échappés, CORS *, corps POST non borné) ET corrigés dans la foulée — échappement via echapper(), CORS restreint localhost/null, Content-Length robuste + borne 64 Kio. Dimension sécurité moyen->ok. XSS vérifié par harnais Edge (FAIL sur le code d avant, PASS après).
- **`VScode5`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ : tests/test_serve_wiki.py créé — 11 tests HTTP réels (serveur sur port éphémère, arbitrages isolé par env) : ping, wiki servi, action déterministe réelle (sync-check), refuser écrit réellement, régression anti-doublon 409 concurrente, +2 tests des corrections sécurité. Dimension test technique 3->4 fichiers.
- **`VScode5`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ : warn_verif_before_commit.py porté sur le hub lui-même (dernier projet à le recevoir), adapté au canal Python/pytest (zones scripts/, tests/, .claude/supervision|orchestration|dispositif|hooks/ ; signaux pytest/py_compile/scan_projets.py), câblé en PreToolUse. Vérifié dans les 2 sens (avertit sans trace de vérif / silencieux si pytest a tourné). Dimension revue-code passe vert.
- **`non_invocation_skills`** (2026-07-24) : ACCEPTÉ + APPLIQUÉ : TestNonInvocationSkills ajoutée à tests/test_canon.py (7 cas) — dont le scénario EXACT qui a cassé 2 fois (skill sans scripts/ ni citation reste jamais-utilisée), + frontière regex, famille BMAD, mention-sans-chemin. Comble l angle mort : finding_arbitre était testée, non_invocation_skills non.
- **`scan-detection-mesure`** (2026-07-24) : ACCEPTE + APPLIQUE (revue incrementale + correctifs non-verts, choix utilisateur = corriger les artefacts de mesure) : le scan ne reconnaissait pas des dispositifs DEJA en place, affichant de faux non-verts. Deux marqueurs ajoutes a scan_projets.py : (1) c8 (avec guillemets, cle package.json) dans la detection coverage -> VSCode1 test-technique 🟠->🟢 (c8 + test:cov reels en devDependencies) ; (2) HTTPServer/serve_forever/urllib.request/http.client/httpx/requests.(get|post) dans FONCTIONNEL_MARQUEURS -> VScode5 test-fonctionnel 🔴->🟠 (tests/test_serve_wiki.py monte un vrai serveur + HTTP), et VSCode2 17->19 (comptage affine). Aucun faux positif (marqueurs appliques aux seuls fichiers de test ; verifie sur les 6 projets). 8 tests de non-regression de la detection ajoutes (tests/test_scan_detection.py, dont anti-faux-positif c8-sur-hash et requests-en-commentaire). Les autres non-verts (cadrage produit, auth PII, monolithes) laisses hors perimetre par choix utilisateur : dette assumee / decision produit, pas des artefacts.
- **`famille:design-review`** (2026-07-24) : ACCEPTE + APPLIQUE (2e vrai manque du plan correctif) : deck-design-review greffee sur VSCode3 — le reste-a-traiter de l arbitrage design-review du 2026-07-23 est solde. REECRITE (R3) pour le canal reel de VSCode3 : generate_deck.py (pas d app/curl), rendu LibreOffice, 40 slides sur 8 chapitres SCALE, contrat par slide ancre sur les vraies fonctions + les 2 gotchas du deck (teardrop CARRE, glyphe ⟲ gras en tofu). Verifie sans fuite VSCode2, 9 fonctions slide citees confirmees dans le generateur. Commit scope pousse.
- **`risque-technique:VSCode2-deps`** (2026-07-24) : ACCEPTE + APPLIQUE (2e vrai manque du plan correctif) : dependances VSCode2 epinglees. requirements.txt + requirements-dev.txt passent de >= a == aux versions REELLEMENT installees dans le venv (pip freeze) — env inchange donc suite verte, mais fresh install reproductible (finding risque_technique audit 2026-07-23). Verifie : pip check OK + dry-run resolvent + suite complete relancee dans le venv APRES epinglage = 330 passed in 218s. Commit scope (2 fichiers, wiki+coverage exclus) pousse 0a5ddda.
- **`audit-technique:VSCode1-VSCode2`** (2026-07-24) : ACCEPTE + APPLIQUE (campagne correctrice flotte, perimetre arbitre = re-auditer VSCode1+VSCode2) : audits du 23/07 perimes rafraichis. VSCode1 risque_technique : finding n1 (coeur de calcul de scores non teste) RESOLU — verifie src/scores.js + scripts/test-scores.js (14 assertions vertes, execute, branche dans npm test). VSCode2 risque_technique : finding deps-non-epinglees RESOLU — 12 deps toutes en == (pip check OK, 330 tests apres epinglage). Les 2 dimensions restent moyen (autres findings honnetes conserves : test-export hors npm test, duplication, singleton db / gros module export, migrations f-string) mais les audits sont a jour et les findings deja corriges fermes. Les autres non-verts de la campagne (quick-wins config, gros refactors, perf, doc) laisses hors perimetre par choix utilisateur.
- **`risque-perf-flotte`** (2026-07-24) : ACCEPTE + APPLIQUE (reduire le risque technique + traiter la performance VSCode1/VSCode2) : 4 chantiers verifies par les faits. (1) VScode5 : garde a la generation du JS du wiki — tests/test_wiki_js.py passe node --check sur le <script> reellement livre + verifie les 2 pieges d echappement payes ; prouvee sur le pattern casse historique (SyntaxError Unexpected identifier elle). (2) VSCode2 ad14dc1 : budget_ok factorise (_budget_lignes), B904 x2 from exc, Whisper/extraction IA sur asyncio.to_thread (boucle d evenements plus gelee), upload backup streame par blocs (plus de 1h30-3h d audio en RAM) — ruff 0 nouveau finding (compare par git stash), 330 passed. Perf VSCode2 moyen->ok. (3) VSCode1 cbe7e4f : N+1 d agregerResultats resolu (1 requete IN groupee + Map repondants), livrable PPT branche dans npm test via un pont qui skip proprement en CI — npm test complet TOUS VERTS dont le test PPT reel. Perf VSCode1 moyen->ok. (4) VSCode : finding package.json PERIME — le manifeste existe deja (c8+node --test), rien touche (R1, correction minimale = aucune). Restes assumes documentes dans les audits (monolithes, migrations f-string, duplication routes, singleton db).
- **`securite:VSCode1-api-pii`** (2026-07-25) : ACCEPTÉ + APPLIQUÉ (bouton Valider du wiki, choix explicite « option C ») : sur le finding sécurité de l'audit VSCode1 du 2026-07-24 (aucune authentification sur l'API qui expose des données nominatives), l'utilisateur a retenu l'option C — implémenter l'Epic 10 complet (US10.1–10.6) comme chantier PRODUIT, pas de barrière provisoire (options A Basic Auth et B jeton d'API écartées). Application conforme au libellé validé (« je ne code rien et le finding reste ouvert avec échéance ») : décision ancrée dans cadrage/epics-us.md de VSCode1 (section Epic 10, commit scopé 00ee664 poussé, churn étranger du dépôt exclu), échéance 2026-08-08 (trancher compte local vs SSO/OIDC et planifier le chantier au prochain diagnostic). Vérifié : revue fraîche OK (sous-agent, diff seul), node --check server.js OK, serveur réellement lancé — GET /api/sessions et /api/roles répondent HTTP 200 SANS authentification (comportement inchangé, preuve que le finding reste factuellement ouvert). La dimension sécurité de l'audit VSCode1 reste « moyen » à dessein jusqu'à la livraison de l'Epic 10.
- **`test-fonct:VSCode1`** (2026-07-25) : ACCEPTÉ + APPLIQUÉ (bouton Valider du wiki, session non interactive) : sur le finding « VSCode1 :: Test fonct. 🟠 — 1 test(s) à vérification réelle » (seul scripts/test-export-ppt.py vérifiait un artefact réel), ajout d'un 2e test fonctionnel bout-en-bout ADAPTÉ AU CANAL (R3 : script Node natif, node:assert/strict, aucun framework) : app/scripts/test-smoke-http.js démarre le VRAI serveur src/server.js en processus enfant sur un port libre avec base SQLite temporaire (convention DB_PATH de test-reimport.js) et vérifie en HTTP réel la page d'accueil (/), la console animateur (/admin.html) et /api/env (APP_ENV restitué). Câblé en fin de chaîne npm test + section Tests du README réalignée sur la chaîne effective. Vérifié PAR LES FAITS : revue fraîche (sous-agent, diff seul — 1 écart bloquant corrigé : rmSync du nettoyage passé en try/catch best-effort, convention test-reimport.js), smoke test rejoué vert, npm test COMPLET vert (11 scripts dont le test PPT réel), commit scopé 3 fichiers (churn docs/wiki/technical/agents-supervision.md exclu, vérifié par git diff --cached --name-only) poussé 1c89950, scan relancé : wiki affiche désormais VSCode1 Test fonct. 🟢 « 2 test(s) à vérification réelle » (et Test tech. passe à 13 fichiers de test).
- **`risque-technique:VSCode2-pptx-export-migrations`** (2026-07-25) : ACCEPTÉ + APPLIQUÉ (bouton Valider du wiki, session non interactive) : sur les 2 findings risque_technique restants de l'audit VSCode2 (« gros module pptx_export 1636 l » et « migrations SQLite en f-string »), remédiation appliquée via evolution-flotte, commit scopé a3ca545 poussé. (1) pptx_export.py (1646 l au moment du cadrage) découpé en package app/services/pptx_export/ de 6 sous-modules cohésifs (base 475 l, images, slides_cadre, slides_diagnostic, slides_trajectoire, build) — code déplacé VERBATIM (3 ajustements de profondeur de chemin seulement), API publique inchangée (build_presentation/field_fit_hint/FIELD_SHAPE), 1 test adapté (monkeypatch des internes image vers le sous-module images). (2) Migrations : app/db.py NON touché (R2 — il porte le travail non commité d'une session concurrente ACTIVE sur le chantier record/extraction) ; à la place, tests/test_db_migrations.py verrouille le mécanisme sur engine jetable monkeypatché (idempotence d'init_db, rattrapage réel d'une base ancienne à colonnes droppées, garde anti-dérive modèles vs schéma) — le refactor du mécanisme lui-même reste à arbitrer quand le chantier concurrent sera committé. Vérifié PAR LES FAITS : py_compile + ruff I001 propres, suite complète 348 passed avec le découpage, export réel de bout en bout (seed démo → PPTX 21 slides 6,4 Mo relu depuis le disque, garde-fou géométrie OK), revue fraîche RAS (verbatim confirmé fonction par fonction, périmètre propre). Les 2 échecs observés ensuite en suite complète (test_interview_libre, test_mission_trame_flow) se reproduisent SANS mon diff : ils appartiennent au chantier concurrent (record.html/pollRepartition), hors périmètre. Audit VSCode2 : risque_technique moyen → ok (dette résiduelle assumée : migrations additives sans rollback, désormais testées).
- **`robustesse:VScode5-content-length`** (2026-07-25) : ACCEPTÉ + APPLIQUÉ (bouton Valider du wiki, session non interactive) : sur le finding robustesse de l'audit VScode5 ("un en-tête Content-Length " malformé lève ValueError hors try/except -> 500 au lieu d'un 400 propre", scripts/serve_wiki.py:312), le cadrage réel a montré que le fix était DÉJÀ committé (8b537e3, 2026-07-24, tests/test_serve_wiki.py::test_content_length_malforme_donne_400_pas_500) — R1, rien à refaire. La revue fraîche du fix existant a trouvé un trou résiduel dans CE MÊME correctif : Content-Length négatif (ex. "-1") passe int() sans ValueError ET passe le garde-fou "length > 65536" (faux pour un négatif), atteignant rfile.read(-1) qui lit jusqu'à EOF -> peut bloquer le thread indéfiniment sur une connexion keep-alive (le même risque de corps non borné que le fix voulait fermer, par une valeur non testée). Corrigé par "length < 0 or length > 65536" (scripts/serve_wiki.py:324) + nouveau test test_content_length_negatif_refuse_sans_bloquer. Vérifié PAR LES FAITS : py_compile propre, suite complète tests/test_serve_wiki.py 12 passed in 2.00s (dont le nouveau test, requête HTTP réelle contre le vrai serveur, aucun blocage observé). Finding robustesse de .claude/audits/VScode5.json mis à jour (marqué CORRIGÉ avec la localisation exacte du gap résiduel et du fix).
- **`securite:VSCode-comop-listen-0.0.0.0`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (bouton Valider du wiki, session non interactive) : sur le finding sécurité de l'audit VSCode 2026-07-23 (seule dimension sécurité non verte de la flotte — « server.listen(port) sans hôte -> écoute 0.0.0.0 » alors que le service spawn PowerShell et accepte l'upload de templates), remédiation appliquée via evolution-flotte. Cadrage réel (R1) : finding confirmé, server.js:390 liait bien sur toutes les interfaces ; le console.log annonçait déjà localhost -> le fix aligne le comportement sur la doc. Fix minimal d'une ligne : server.listen(port) -> server.listen(port, '127.0.0.1', ...). port reste lu depuis process.env.PORT (inchangé). Vérifié PAR LES FAITS : (1) revue fraîche sous-agent sonnet sur le diff seul = RAS (fix correct, périmètre propre, aucun autre fichier embarqué) ; (2) test technique node --check OK + suite node --test 10/10 (les tests montent le vrai serveur et frappent les endpoints HTTP sur loopback -> serveur toujours fonctionnel) ; (3) test fonctionnel PAR LES FAITS : serveur réellement lancé sur port éphémère, netstat montre TCP 127.0.0.1:PORT LISTENING (plus 0.0.0.0:PORT), GET http://127.0.0.1 = HTTP 200, GET http://192.168.1.196 (IP LAN) = connexion REFUSÉE (était atteignable avant le fix) ; (4) smoke-test.ps1 rejoué : 2 échecs de golden-file du template PROUVÉS hors périmètre (mêmes 2 échecs à l'identique avec server.js remis à HEAD via git stash — dérive du template, le smoke-test n'exécute jamais server.js). Commit scopé 563388f (git diff --cached --name-only = comop-pptx-prototype/server.js UNIQUEMENT, churn supervision/wiki du dépôt cible exclu R2) poussé sur origin/main. Audit VSCode dimension sécurité moyen -> ok (finding marqué CORRIGÉ ; surface résiduelle assumée : zip-slip théorique). Toute la flotte a désormais sa dimension sécurité verte.
- **`cadrage-produit:VSCode3`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (bouton Valider du wiki, session non interactive) : sur le finding « VSCode3 :: Cadrage produit — why » (dimension cadrage produit 🟠, seul le marqueur « why » détecté), le cadrage réel (R1) a montré que le manque n'était PAS le fond — bmad-iap-cadrage.md ~1100 l (v2.3) + revue-produit-marche.md couvrent persona/pourquoi/besoins/valeur en profondeur — mais l'absence d'une porte d'entrée produit stable, aggravée par le fait que le scan tronque chaque fichier à 2000 c (persona/besoins/valeur vivaient trop bas dans le cadrage pour être vus). Remédiation R1/R3 (correction minimale, adaptée au canal, PAS une réécriture du cadrage) via evolution-flotte : création de docs/product-brief.md sur VSCode3 — synthèse 1 page renvoyant au cadrage comme source de vérité. Persona = sponsor DSI/direction infra + équipes plateforme + consultant opérateur ; pourquoi = 3 déclencheurs (infra subie intenable, écart adoption/valeur 80/30, IA-organisation-d'abord) ; besoins & douleurs = capacité humaine rare drainée par le gaspillage, plateforme peu adoptée, fatigue de gouvernance IA ; proposition de valeur = double mission couplée Transformer + Assainir (différenciateur = le couplage produit+gaspillage+doctrine IA, pas l'étiquette déjà prise par Thoughtworks/Itential). Honnêteté épistémique préservée (finding critique C1) : financement croisé présenté en hypothèse instrumentée à prouver, jamais en fait acquis, même statut au sponsor qu'en interne (pas de double discours). Vérifié PAR LES FAITS : analyse_pratiques(VSCode3).cadrage_produit exécuté → niveau ok (persona, why, besoins, valeur + brief BMAD, score 5) ; scan complet relancé → wiki.html rend la pastille 🟢 (était 🟠 « why »). Revue fraîche en contexte frais (sous-agent, brief seul + sources) : C1 respecté, tous les chiffres fidèles (~29 % Flexera, 80/30 Gartner, >40 % abandons 2027, 87 %/25 %), 2 écarts mineurs de fidélité corrigés (Grille V3.2 requalifiée « référentiel déjà éprouvé » sans nom interne ; discipline des tags dans le deck sponsor marquée recommandation ouverte M1). Commit scopé docs/product-brief.md SEUL (churn étranger docs/wiki* du dépôt cible exclu R2, vérifié par git diff --cached --name-only) poussé d871cc6 sur origin/main.
- **`VSCode :: Pratiques+rules — CLAUDE.md`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (bouton Valider du wiki, session non interactive) : sur le finding « VSCode :: Pratiques+rules — CLAUDE.md » (seul marqueur présent de la dimension, à 🟠 moyen), le cadrage réel (R1) a montré que le CLAUDE.md du dépôt VSCode était un SQUELETTE de 36 l : sa section « Projet » était un placeholder explicitement non rempli (« Contexte détaillé à compléter — pas encore rédigé par l'équipe ») et il ne mentionnait NULLE PART le canal réel primaire du dépôt — génération PPT via COMOP + PowerShell, la leçon même qui a fondé la règle R3 « adapter au canal » de la flotte. Remédiation minimale et fidèle au canal (PAS de linter/CI plaqués — hors périmètre VSCode « peu de code » par l'arbitrage famille:linter/tests du 2026-07-23, non ré-ouvert) : réécriture du CLAUDE.md (69 l, < 150 — règle veille) qui (a) résout le placeholder avec des faits réels (prototype comop-pptx-prototype/ + dispositif de supervision .claude/), (b) documente le canal COMOP (server.js Node port 5177 bind 127.0.0.1, scripts src/*.ps1, vérifs npm test/smoke-test.ps1/validate-template.ps1, garde-fou safeTemplatePath, coverage c8 ~50 %), (c) répare 4 pointeurs cassés/périmés remontés par la revue fraîche + ma vérif fonctionnelle : « docs/ vide » (faux → docs/wiki synchronisé du hub), « pas de .claude/agents/ » (faux → pointe l'agent custom réel ppt-designer.md), réf docs/vscode1-export/optimisation-tokens.md (inexistant ici → source OCTO Playbook), .agents/skills/ (inexistant → mention retirée). Vérifié PAR LES FAITS : revue de code en contexte frais (sous-agent sonnet, diff seul — 2 findings bloquants remontés PUIS re-vérifiés clos) ; test fonctionnel d'un doc de règles = chaque pointeur doit résoudre → les 19 chemins cités testés existent TOUS, le chemin déclaré absent (.agents/skills) confirmé absent ; < 150 l ; commit scopé au SEUL CLAUDE.md (churn supervision routing-hints/state/docs-wiki exclu R2, vérifié par git diff --cached --name-only) poussé 61710e6 sur origin/main. Note honnête : la pastille Pratiques+rules reste 🟠 moyen (score inchangé — linter/CI/conventions hors périmètre par arbitrage antérieur, dette assumée) ; c'est le CONTENU du CLAUDE.md — l'artefact nommé par le finding — qui est désormais complet, exact et adapté au canal.
- **`bmad-catalogue-codex:VSCode`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (bouton Valider du wiki, session non interactive) : sur le finding du diagnostic LOCAL de VSCode « bmad-catalogue-codex — Duplication .agents/skills/ (Codex) : poids mort structurel », le cadrage réel (R1) a établi que la remédiation proposée (réinstaller BMAD --tools claude-code uniquement + retirer .agents/skills/) était DÉJÀ appliquée et committée de longue date : commit 5c480d1 « chore(bmad): reinstalle en claude-code-only, retire la copie Codex » (dizaines de fichiers .agents/skills/bmad-* supprimés), déjà consigné dans l'arbitrages.json du DÉPÔT CIBLE sous la cible sœur « .agents/skills » (2026-07-23). Cause du finding resté affiché ouvert : la fermeture du scan (scan_projets.py, findings du diagnostic filtrés par les cibles d'arbitrages.json en égalité de chaîne) ne rapprochait pas « bmad-catalogue-codex » de « .agents/skills ». Correction minimale (R1) — AUCUNE suppression refaite, AUCUN fichier code/config touché : ajout dans l'arbitrages.json de VSCode d'une entrée à la cible EXACTE « bmad-catalogue-codex » qui clôt le finding. Vérifié PAR LES FAITS : (1) .agents/ absent du disque et 0 fichier suivi par git dans le dépôt VSCode ; (2) aucun manifeste/config _bmad ne référence codex (les mentions « Codex » résiduelles = contenu documentaire de skills claude-code sur les runtimes MCP, pas une install dupliquée) ; (3) CLAUDE.md documente déjà l'install mono-outil ; (4) reproduction de la logique de fermeture du scan sur les données réelles → finding filtré (2 findings locaux restants : revue-increment, scan_transcripts, hors périmètre) ; (5) revue fraîche en contexte frais (sous-agent, diff + faits seuls) : toutes affirmations VRAIES, aucun écart bloquant, 1 imprécision de comptage corrigée. Commit scopé au SEUL .claude/supervision/arbitrages.json de VSCode (4 fichiers de churn généré — routing-hints/state/docs-wiki — exclus R2, vérifié par git diff --cached --name-only) poussé 979155c sur origin/main. Suite : la pastille d'alerte de VSCode dans le wiki de supervision retombe au plus haut des findings restants (p3) une fois le scan relancé.
- **`wiki-verite`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (« traite P4, P3 et P2 ») : les deux défauts de vérité du wiki corrigés dans scan_projets.py. (a) Marqueurs TODO-AGENTS-HTML réintroduits dans le template à onglets (bloc en fin d'onglet Pilotage), avec PRÉSERVATION du dernier bloc injecté par scan_transcripts.py à chaque régénération — vérifié par les faits : scan_transcripts.py écrit à nouveau wiki.html sans avertissement (« -> agents-supervision.md, index.md ET wiki.html »). (b) Cellules CI et linter du répertoire craft DÉRIVÉES de la mesure (craft_effectives, depuis pratiques_rules) au lieu d'un texte figé — le wiki affiche désormais « CI détectée sur VSCode1, VSCode2 (2/6) » et « Linter détecté sur VSCode1, VSCode2, VScode5 (3/6) » ; cellule deps VSCode2 recalée (épinglé ==). 10 tests de non-régression (tests/test_wiki_verite.py), suite complète 80 passed.
- **`VScode5`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (« traite P4, P3 et P2 ») : skill revue-increment RÉÉCRITE pour le canal du hub (R3 — vérité du journal via log_run --solde, arbitrages à la cible exacte, wiki régénéré et regardé, suite pytest, commits scopés R2 sur la flotte, données générées jamais éditées à la main) + hook remind_revue_increment.py câblé en SessionStart (fail open vérifié dans les 2 sens : contexte injecté sur entrée valide, silencieux sur entrée invalide). Dimension revue d'incrément du hub : 🔴 absente -> 🟢 skill + hook SessionStart (prouvé au scan).
- **`VScode5`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (« traite P4, P3 et P2 ») : pytest-cov installé et épinglé (requirements-dev.txt créé : pytest==9.1.1, pytest-cov==7.1.0), commande documentée dans CLAUDE.md (table des vérifications), .coverage/htmlcov gitignorés. PREMIÈRE MESURE RÉELLE : 24 % TOTAL (log_run 83 %, serve_wiki 65 %, scan_transcripts 16 %, scan_projets 13 %) — aucun seuil imposé, on mesure d'abord (modèle VSCode2). Dimension test technique du hub : 🟠 -> 🟢 « 7 fichiers de test, coverage configuré » (prouvé au scan). L'arbitrage demandé par le run P2 du 2026-07-24 est soldé.
- **`skills-jamais-utilisees`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (« traite P4, P3 et P2 ») : mesure corrigée AVANT tout tri. Diagnostic par les faits : aucun événement Skill/command-name pour veille-agentic dans les 92 transcripts — le compteur fonctionnait, la classification était fausse (exécution inline / consommation par lecture, invisibles structurellement). Fix : skills_reference.json (déclaration par ARBITRAGE, versionnée : deck-design-library, restitution-deck-design, veille-agentic) lu par le canon (skills_reference_declares, fail open), 6 tests de non-régression dans tests/test_canon.py. Le chantier a aussi résorbé une dérive canon découverte en chemin : 10 copies flotte en retard sur le canon (enrichi detector v2 par la session concurrente du jour) — sync 12/12 à jour, rejeu v2 exécuté au hub (+125 événements recomptés). Les 3 skills sont désormais en bibliotheque_reference, plus en jamais_utilises. Statu quo BMAD maintenu jusqu'à la v7 : aucune customisation.
- **`VSCode4`** (2026-07-27) : ACCEPTÉ + APPLIQUÉ (« traite P4, P3 et P2 », option A) : docs/product-brief.md créé sur VSCode4 via evolution-flotte — porte d'entrée 1 page ancrée sur les sources réelles (docs/wiki/rh-ecoute.md sourcée + deck 15 slides de generate_deck_ohc.py). Persona = board P&D / consultants P&D ; pourquoi = 3 des 4 formats proposés réactivent Zen@octo 2016, « réactiver et rendre visible, pas recréer » ; besoins = stress, missions complexes sans mentorat, confusion des rôles, neutralité ; valeur = deck actionnable qui arbitre (GARDER/MODIFIER×2/SUPPRIMER) et séquence le pilote de septembre 2026. Honnêteté épistémique : verdicts présentés en propositions à valider par le board P&D, jamais en décisions acquises. Revue fraîche en contexte frais (sous-agent sonnet, sources seules) : 1 écart BLOQUANT corrigé (causes audit ISIS 2016 : organisation/management/communication — j'avais écrit relation client/charge par contamination avec l'auto-diagnostic mentorat) + 3 imprécisions corrigées. Vérifié par les faits : analyse_pratiques(VSCode4).cadrage_produit = ok (persona, why, besoins, valeur + brief BMAD) ; wiki 🔴 -> 🟢. Commit scopé bbc56f2 poussé (churn wiki/Exports du dépôt cible exclu R2).
- **`VSCode:increment-6-pptx`** (2026-07-28) : ACCEPTÉ + APPLIQUÉ (« traite p5, p4 et p3 ») — option A du finding, chantier cause-racine. Le finding p5 disait vrai : la régression PPTX rouverte le 2026-07-23 (commit 8833ed1) n'avait reçu aucune ligne de travail. Traitée ici par REPRODUCTION avant tout correctif (méthode imposée par la mémoire feedback_regression_step_back), via evolution-flotte sur le dépôt VSCode. DIAGNOSTIC : la cause suspectée par la roadmap (pattern zip extraction/mutation/repackage) est FAUSSE — remove-template-shape.ps1, le script de l'incrément 6, produit un paquet sain (vérifié : 0 XML invalide, ouvert dans PowerPoint). Les vraies causes sont dans apply-octo-branding.ps1, en place depuis l'incrément 1 : (1) Invoke-ThemeBranding ré-emballait le groupe capturé dans des chevrons — le remplacement « <$1><a:latin… » avec $1 = « <a:majorFont> » écrivait « <<a:majorFont>> » dans ppt/theme/theme1.xml, partie XML non parsable donc fichier refusé en bloc par PowerPoint ; (2) branding appliqué EN PLACE et non idempotent : chaque passe réinjectait OctoFooter/OctoPageCircle/OctoAccentLine avec les mêmes p:cNvPr id 9900-9902 → ids dupliqués dès la 2e passe. Le défaut était structurellement invisible : taille normale, 126 entrées zip, python-pptx ouvre sans broncher (il ne lit pas le thème) — d'où 45 jours sans détection. CORRECTIFS : les 2 causes corrigées dans apply-octo-branding.ps1 ; le template committé (qui portait déjà le défaut 1, donc contaminait TOUTE génération) réparé sur theme1.xml UNIQUEMENT — prouvé minimal : 125 des 126 parties octet pour octet identiques à HEAD, la 126e à −4 octets (les deux chevrons en trop). GATE MANQUANTE AJOUTÉE — c'est le vrai manque du projet : rien n'ouvrait le fichier produit. src/verify-pptx-integrity.ps1 (toutes parties XML parsables + aucun p:cNvPr id dupliqué par slide ; option -RealOpen = ouverture réelle PowerPoint par COM) + test/test-pptx-integrity.js qui la rejoue dans npm test sur le vrai template, sur un template rebrandé 2 fois, et sur un paquet volontairement cassé (prouve que la gate mord). VÉRIFIÉ PAR LES FAITS, avec l'oracle de l'utilisateur : PowerPoint REFUSE le template d'avant (« Le fichier ou le répertoire est endommagé et illisible », HRESULT 0x80070570) et OUVRE celui d'après ; deck COMOP généré de bout en bout et ouvert dans PowerPoint (3 slides) ; npm test 13/13 ; les 2 échecs de smoke-test.ps1 reproduits à l'identique sur HEAD sans le diff (dérive de golden-file préexistante, hors périmètre). Revue fraîche en contexte frais (sous-agent sonnet, diff seul, tests réexécutés) : aucun écart bloquant. Commit scopé badedf1 poussé (churn .claude/ exclu R2). ROADMAP mise à jour (json + svg régénéré) : l'incrément 6 reste 'current' À DESSEIN — le flipper en 'done' sans que l'utilisateur ait ouvert le deck répéterait exactement la faute du 2026-06-08.
- **`docs/reflexions`** (2026-07-28) : ACCEPTÉ + APPLIQUÉ (« traite p5, p4 et p3 ») : la fuite est structurelle — une proposition écrite dans un document de réflexion n'a ni bouton Valider ni suivi, alors qu'un finding de diagnostic.json en a un. Preuve vérifiée le 2026-07-28 : du lot « S » de solutions-risque-technique.md (2026-07-24), 1 item sur 6 appliqué en 4 jours, quand les 5 findings du diagnostic du 2026-07-27 l'étaient à 5/5. (a) Volet structurel : la skill agent-supervisor (§2, signaux hors étage 1) lit désormais docs/reflexions/*.md — toute proposition non reprise dans un arbitrage depuis plus de 7 jours doit être re-vérifiée dans le code réel puis, si elle tient, REVERSÉE EN FINDING avec sa preuve. (b) Volet rattrapage, fait dans le même mouvement : l'état réel des 6 items du lot S a été vérifié un par un (pas cru sur parole). #4 CI VSCode est traité par le p3 de ce même run ; #5 routes VSCode1 était déjà appliqué (2026-07-25) ; #2 migrations VSCode2 reste atténué-et-testé, re-arbitrable quand le chantier concurrent aura atterri. Les 3 qui tiennent toujours — #1 JS du wiki inline, #3 matrice de divergence pptx_deck, #6 garde template VSCode3 — sont reversés en findings du diagnostic, donc arbitrables depuis le wiki.
- **`famille:linter-ci-decks`** (2026-07-28) : ACCEPTÉ + APPLIQUÉ (« traite p5, p4 et p3 ») : le périmètre d'exclusion « VSCode/VSCode3/VSCode4 = peu de code » des arbitrages famille:linter (2026-07-23) et famille:integration-continue (2026-07-24) est RE-ARBITRÉ sur la mesure, qui le dément (VSCode3 : 2767 l ; VSCode4 : ~2100 l + 930 l de fork pptx_deck ; VSCode : 5 fichiers de test et coverage c8 que rien ne rejouait). (a) CI GitHub Actions sur VSCode (.github/workflows/ci.yml, runner windows-latest OBLIGATOIRE car les scripts de mutation sont en PowerShell) : npm ci + npm test — donc la gate d'intégrité OOXML du p5 — plus la couverture c8 en non bloquant. C'est la solution #4 du lot S. CI flotte : 2/6 → 3/6. (b) ruff sur VSCode3 et VSCode4 : pyproject.toml aligné sur la baseline du hub et de VSCode2 (F/I/UP/B, line-length 100, _bmad et .claude/skills exclus). PREMIÈRES MESURES RÉELLES : VSCode3 = 11 points (6 I001, 4 B905, 1 F541), VSCode4 = 8 points (5 I001, 2 UP009, 1 F541). Aucun seuil imposé, AUCUN --fix (un --fix aveugle avait supprimé un ré-export et cassé un import sur VSCode2 le 2026-07-23) ; commande documentée dans les deux CLAUDE.md, qui restent sous la limite de 150 lignes (111 et 137). Linter flotte : 3/6 → 5/6. Commits scopés poussés : VSCode badedf1, VSCode3 2f69b34, VSCode4 3d6aba8 (churn docs/wiki des dépôts cibles exclu R2, vérifié par git diff --cached --name-only).
- **`playbook:evolution-flotte`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« arbitre les 5 findings ») : le contrat est fermé aux deux bouts. (a) Étape terminale OBLIGATOIRE revue-increment ajoutée aux playbooks evolution-flotte ET dev-verifie — même statut que l'étape revue-fraiche ancrée le 2026-07-24 ; allégeable en fin de campagne (une revue pour plusieurs runs de la même séance), jamais sautée. (b) Avertissement NON bloquant dans le canon log_run.py : un run orchestré journalisé 'succes' sans étape revue-increment au plan (ni trace dans notes) est signalé — on mesure d'abord combien de runs le déclenchent avant tout garde-fou dur. 5 tests de non-régression (TestAvertissementRevueIncrement), suite test_canon 42 passed, sync canon 12/12, propagé et committé scopé sur les dépôts cibles. (c) Critère commit-scope amendé : message de commit écrit dans un FICHIER puis git commit -F — jamais de here-string/heredoc pour un message portant apostrophes/backticks/$ (3 des 12 reprises du journal venaient de cette famille : heredoc bash 2026-07-24, here-string PowerShell 2026-07-27, collision de casse realOpen/RealOpen 2026-07-28) — la leçon payée 3 fois est désormais écrite dans le contrat.
- **`famille:cadrage-produit-apps`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« arbitre les 5 findings ») : docs/product-brief.md créé sur VSCode2 (commit 6cc3d0d) et VSCode1 (commit 8879c07) via evolution-flotte — synthèse 1 page ancrée sur l'existant (modèle VSCode3/VSCode4), AUCUNE réécriture de cadrage (R1). VSCode2 : persona consultant·e entretiens qualitatifs (libellé exact du README), chaîne capture→synthèse 5 catégories→PPT vérifié, différenciateur = couplage bout-en-bout local (confidentialité des interviews). VSCode1 : 4 personas fidèles à cadrage/personas.md, et la décision Epic 10 (auth/PII) consignée comme RISQUE PRODUIT DATÉ — échéance 2026-08-08, renvoi à l'arbitrage securite:VSCode1-api-pii — plus une dette technique isolée. Revue de fidélité en contexte frais : 3 écarts bloquants corrigés (persona 'responsable de mission' inventé retiré ; 'seul·e ou en binôme' non ancré retiré ; 4e persona Sponsor/RH/Direction réintégré) + 2 mineurs (libellés exacts des catégories ; citation 'option C' re-sourcée au hub). Vérifié par les faits : analyse_pratiques(cadrage_produit) = ok sur les DEUX projets (persona, why, besoins + brief). Commits scopés poussés, churn concurrent exclu (chantier extraction IA actif sur VSCode2 non touché).
- **`pptx_deck:matrice-divergence`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« arbitre les 5 findings ») : matrice de divergence ajoutée à l'étage déterministe du scan (ast, 0 token) — fonctions top-niveau par copie, communes, propres à chacune, écarts de signature sur les communes — rendue dans la section « Pratiques, couverture & risques » du wiki (html + md). PREMIÈRE MESURE RÉELLE : VSCode2 608 l / 29 fonctions, VSCode3 305 l / 16, VSCode4 930 l / 41 dont 12 PROPRES (le fork réel pressenti par la leçon P1 est confirmé par les chiffres) ; 16 fonctions communes aux 3, 1 signature divergente (add_card — VSCode4 a étendu la signature). Garde-fous : copie absente ou non parsable affichée sans casser le scan. 9 tests (tests/test_divergence_pptx_deck.py) sur copies synthétiques + page livrée. La dette n°1 de la flotte est désormais chiffrée à chaque scan — l'extraction d'un cœur commun (item #10 du lot S) reste un chantier à arbitrer SÉPARÉMENT, sur cette mesure.
- **`VScode5:js-inline-wiki`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« arbitre les 5 findings ») : le JS du wiki est extrait de scan_projets.py vers docs/wiki_app.js (286 lignes, fichier réel versionné, node --check natif), inliné à la génération par lire_wiki_app_js() (garde : fichier absent = SystemExit nommant le chemin, jamais une page sans boutons). Les valeurs dynamiques passent par un unique bloc <script id=wiki-config type=application/json> sérialisé via json.dumps — plus AUCUNE interpolation de chaîne Python dans du code JS, la classe de bugs est éliminée à la racine. Leçon confirmée en migrant : la 1re extraction (source Python brut) a produit un \' cassé — exactement le bug historique — attrapé par node --check ; ré-extrait depuis la chaîne ÉVALUÉE de wiki.html. tests/test_wiki_js.py conservé tel quel (filet de la migration, vert) + 3 nouveaux tests (source node --check, inline = fichier exact, config JSON valide). Suite complète 103 passed.
- **`VSCode3:garde-template`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« arbitre les 5 findings ») : garde _exiger_template() dans docs/cadrage-ppt/generate_deck.py de VSCode3 — contrôle d'existence AVANT chaque Presentation(TEMPLATE) (import ligne ~109 et new_prs(), les 2 seuls sites, vérifié par grep), SystemExit nommant le fichier attendu (template-octo.pptx) et son emplacement complet au lieu d'une FileNotFoundError brute. 3 tests (tests/test_generate_deck_garde.py : message sur chemin volontairement absent via monkeypatch du vrai module, chemin nominal inchangé). Revue fraîche en contexte frais : RAS (diff strictement additif +14). Commit scopé b5190f6 poussé (churn wiki exclu R2). Les 3 échecs de tests/test_agent_supervision.py observés sont PROUVÉS hors périmètre (mêmes échecs à l'identique avec generate_deck.py remis à HEAD via git stash) : tests locaux en retard sur le canon enrichi (champ en_cours, findings arbitrés barrés) — signalé au diagnostic suivant, non corrigé ici (hors arbitrage).
- **`sync-canon:checklist-hub`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« traite l'incident sync assumé ») — ferme le « RESTE CÔTÉ HUB » explicitement laissé ouvert par l'arbitrage sync-canon de VSCode2 du même jour (leur gate couvre les commits de la CIBLE ; il manquait le geste côté SOURCE). sync_dispositif.py liste désormais, après tout sync ayant réellement écrit, la commande pytest des suites locales de chaque cible qui exercent un script du canon — détection par lecture des fichiers tests/test_*.py, pas une liste en dur qui se périmerait. Ce n'est PAS une checklist documentaire : c'est l'outil qui parle, au moment exact où le risque naît. Sortie réelle : VSCode2 -> 3 suites, VSCode3 -> 2, hub -> 2. Consigne appliquée dans la foulée : VSCode2 48 passed, VSCode3 32 passed, hub 118 passed (2 passes). 5 tests de non-régression. L'incident s'est REJOUÉ pendant le correctif — des emojis dans un print du canon ont fait échouer le décodage cp1252 des tests cibles (stdout None) : stdout du canon repassé en ASCII strict, et la revue fraîche en contexte frais a trouvé un 2e trou que je n'avais pas vu (le texte libre `demande` du journal porte déjà un U+FFFD hérité d'un mojibake) -> échappement ASCII des données interpolées + 2 tests d'encodage. Le correctif a donc été prouvé par l'incident qu'il documente.
- **`veille:agent-teams`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« traite les actions correctives identifiées ») : l'action corrective de la pratique de veille du 2026-07-29 (doc officielle Anthropic « agent teams ») est appliquée à la skill agent-orchestrator, section 2 ter. Critère de choix écrit noir sur blanc : les SOUS-AGENTS restent le défaut (résultat rendu au demandeur, aucun échange entre eux, coût bas) ; une AGENT TEAM ne se justifie que si les travailleurs doivent se coordonner ou se contredire (revue multi-angles avec débat, hypothèses concurrentes, chantier transverse par couche). Garde-fous officiels reportés : expérimental et désactivé par défaut (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1), coût linéaire par équipier (chacun est une session complète), 3-5 équipiers, 5-6 tâches/équipier, partition STRICTE des fichiers. RIEN N'EST ACTIVÉ : la variable d'environnement n'est pas posée — seul le critère de décision est documenté. Entrée de veille passée `nouveau` -> `adopte`.
- **`pratiques-rules:VSCode1-claude-md`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« corrige la pratique en écart pour VSCode1 ») : le CLAUDE.md dépassait le seuil de 150 lignes adopté le 2026-07-24 (152 l, signalé ⚠ « à élaguer » par le scan) — un CLAUDE.md qui enfle fait ignorer les règles importantes, c'est la raison même du seuil. Élagué à 147 l par COMPRESSION, pas par suppression de substance : sections « Skills & agents » et « hiérarchie de modèles » fusionnées et resserrées, hooks décrits en 4 puces au lieu de 12 lignes, intro documentaire raccourcie (et enrichie du renvoi au product-brief créé le même jour). Vérifié par revue fraîche en contexte frais : les règles DURES R1-R4 sont intactes, le « piège à ne pas recréer » (.opencode/agents supprimé mais .opencode/skills conservé) est toujours là, et les 2 seules pertes relevées sont des détails redérivables d'un clic dans settings.json (liste des chemins secrets, matcher du hook) — exactement ce qu'un élagage doit enlever. Dimension pratiques+rules : le ⚠ disparaît, détail mesuré « linter, CI, CLAUDE.md, conventions ».
- **`pratiques-rules:VScode5`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« corrige les 3 pratiques en écart pour VScode5 » — 1/3) : la dimension était 🟠 avec 2 marqueurs sur 4 (linter, CLAUDE.md). (a) CI GitHub Actions ajoutée (.github/workflows/ci.yml, push main + PR) : ruff informatif, pytest AVEC couverture comme gate — le hub écrit sur les 5 autres dépôts, une régression de son dispositif se propage à toute la flotte, c'est le projet où la CI manquait le plus. Modèle VSCode2, adapté au canal (cible = scripts/ et .claude/dispositif/canon, pas app/). CI flotte : 3/6 → 4/6. (b) docs/wiki/technical/conventions.md créé — le « comment coder » du hub, distinct des règles de gouvernance R1-R5 du CLAUDE.md : linter sans --fix aveugle, verdict de test lu sur la vraie ligne (avec le contournement --basetemp du poste), LA règle du canon (corriger dans le canon, rejouer les suites cibles, stdout ASCII strict), données générées, vocabulaire FR du domaine, commit -F et scope R2. Chaque convention est ancrée sur une reprise réellement payée. Dimension 🟠 → 🟢 (prouvé au scan : « linter, CI, CLAUDE.md, conventions »).
- **`robustesse:VScode5-jobs`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (2/3) : les 2 findings robustesse ouverts de l'audit du 2026-07-24 sont corrigés dans scripts/serve_wiki.py. (a) _run_job rendait « erreur (...) » pour toute cause — on ne savait pas si l'interpréteur manquait, si le script avait disparu ou s'il avait planté : erreurs désormais TYPÉES (FileNotFoundError / PermissionError / OSError / inattendue), le libellé nomme le binaire fautif. (b) JOBS croissait sans borne : plafond JOBS_MAX=200, purge des terminés les plus anciens, et un job EN COURS n'est JAMAIS purgé (son thread écrit encore dedans) — la purge s'exécute sous JOBS_LOCK, vérifié en revue fraîche (pas de reprise du verrou, pas de deadlock). 4 tests dédiés (TestRobustesseJobs), suite serve_wiki 16 passed. Audit : robustesse moyen → ok.
- **`risque-technique:VScode5-js-wiki`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (3/3) — aucun code écrit ici : les 2 findings risque_technique de l'audit étaient DÉJÀ résolus par le chantier js-inline-wiki du matin même (R1, correction minimale = aucune). Fermeture prononcée sur des FAITS re-vérifiés, pas sur la mémoire du run : docs/wiki_app.js existe, son contenu se retrouve tel quel inliné dans wiki.html, 0 concaténation `"""<script>` restante dans scan_projets.py, lire_wiki_app_js() est bien le chemin de génération, et tests/test_wiki_js.py porte 6 tests (node --check sur la source ET sur le JS livré, égalité source↔inline, validité du bloc JSON wiki-config). L'audit est mis à jour avec ces preuves : risque_technique moyen → ok. Dette résiduelle assumée et écrite : scan_projets.py reste un gros module (~2200 l), mais il ne mélange plus deux langages. VScode5 a désormais ses 4 dimensions d'audit vertes.
- **`commande:adopte`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (« active le principe si la commande adopte est transmise à agent-orchestrator ») : `adopte <trouvaille>` devient le VERBE D'ARBITRAGE DE LA VEILLE, symétrique de « applique le finding » pour le diagnostic — section 2 quater de la skill agent-orchestrator. La commande déclenche 6 étapes contractuelles : retrouver l'entrée dans veille.json (ambiguë → demander, jamais deviner) ; cadrer sur l'état réel (R1 : la trouvaille peut être déjà satisfaite) ; appliquer les DEUX débouchés — regle_proposee → référentiel criteres-pratiques.md + outillage du scan quand c'est mesurable à froid (c'est ce qui fait passer un critère ⬜ en ✅), action_corrective → correctif via evolution-flotte sur un autre dépôt ou édition directe sur le hub ; vérifier par les faits ; TRACER deux fois (statut de l'entrée + arbitrage à la cible veille:<slug> — sans ce second, le wiki continue d'afficher la trouvaille en attente) ; journaliser avec `resolution: adoption <nom>`. Garde-fous : jamais d'exécution de code téléchargé, jamais d'activation d'une capacité expérimentale par défaut (documenter le critère vaut adoption, poser la variable d'environnement est une décision séparée), et une pratique déjà généralisée se CONSTATE au lieu de produire un diff cosmétique. « écarte X » suit le même chemin avec sa raison. 7 trouvailles sont adoptables en l'état.
- **`veille:voltagent-subagents`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (commande « adopte » — choix utilisateur explicite parmi 4 trouvailles proposées) : le référentiel public des 154 sous-agents a servi de GRILLE DE COMPARAISON pour la flotte de 17 sous-agents de VSCode1, pas de source à copier. Cadrage réel d'abord (R1) : les 17 agents locaux couvrent audit (2), debug, dev (3 variantes), doc, onboarding, orchestration (2), reconnaissance, planification, deck, QA, revue, UI et UX. Confrontés aux 10 catégories du référentiel, les manques réels sont : sécurité (cat. 04 — security-auditor, penetration-tester), accessibilité (accessibility-tester), données (data-analyst, database-optimizer). GREFFE VOLONTAIREMENT MINIMALE — UN SEUL agent ajouté : le finding agent-mort du 2026-07-28 mesure que 11 des 17 agents n'ont JAMAIS été invoqués ; importer en masse aurait aggravé exactement ce que la flotte cherche à réduire. Le critère retenu pour greffer : un cas d'usage DATÉ et IMMINENT. Un seul le remplit — la sécurité : l'API expose des données nominatives sans aucune authentification (audit du 2026-07-25, seule dimension d'audit non verte du projet), avec une échéance de conception au 2026-08-08. .claude/agents/security-auditor.md créé, RÉÉCRIT pour le canal réel de VSCode1 (R3 : Express/node:sqlite/exports PPT, pas un copier-coller du référentiel) : lecture seule stricte, rapport localisé fichier:ligne par sévérité, preuve exigée (requête réelle plutôt que supposition), rattachement des remédiations aux US de l'Epic 10 — et deux interdits explicites hérités des arbitrages : ne jamais proposer de barrière d'authentification improvisée (Basic Auth et jeton d'API ont été écartés le 2026-07-25), ne jamais déclarer la dimension sécurité résolue sur un correctif partiel. Vérifié : les 4 SKILL.md cités et les 3 chemins de code cités existent tous. Les 3 autres manques (accessibilité, données) restent NON greffés : pas de cas d'usage daté — ils alimentent la décision de tri groupée du 2026-08-16, qui tranchera la mise en sommeil des agents jamais appelés.
- **`flotte:rtk`** (2026-07-29) : ACCEPTÉ + APPLIQUÉ (demande directe utilisateur : « retire rtk de tous les projets ») : proxy token RTK retiré de toute la flotte — skill rtk-usage supprimée (VSCode1, VSCode2), permissions Bash(rtk *) ôtées (settings VSCode2 + settings global), références actives purgées des skills OpenCode (pathfinder, onboarder, dev-standards-testing), des playbooks dev-verifie (VSCode1 + VSCode2 source et export), de revue-increment (source + export), du README export et des docs optimisation-tokens (VSCode1/2/3/4). Mesures historiques datées conservées (vérité du journal) ; copie vendored external/openhub (source upstream du plugin) laissée intacte ; binaire winget rtk.exe et ~/.claude/RTK.md hors périmètre projets, non touchés. Commits : VSCode1 9fd02ad, VSCode2 f6504bb, VSCode3 f9da1e3, VSCode4 1c96638.
- **`VScode5:reliquat-non-verifie`** (2026-07-30) : ACCEPTÉ + APPLIQUÉ (« traite tous ces sujets ») : le reliquat découvert au démarrage (scripts/scan_projets.py + usage.jsonl staged, correctif du finding wiki:finitions-lisibilite jamais vérifié/arbitré/journalisé) est soldé. Vérifié PAR LES FAITS : suite complète relancée (152 puis 158 passed après ajout de tests/test_wiki_finitions.py, 6 tests sur tronque() dont le cas de repli sur coupe brute) ; rendu wiki régénéré vérifié porter carte-lecture/date-audit/file-meta. Finding clos.
- **`wiki:actions-irreversibles`** (2026-07-30) : ACCEPTÉ + APPLIQUÉ (« traite tous ces sujets ») : les 4 volets de la proposition appliqués. (a) confirm() nommant le dépôt cible avant le fetch de l'action valider (docs/wiki_app.js). (b) déduplication serveur étendue à audit (par projet)/diagnostic/veille/reflexion (globales) dans scripts/serve_wiki.py, jusque-là limitée à 4 actions sur 8. (c) route /api/cancel/<job> + bouton Annuler : taskkill /F /T (pas un simple proc.terminate() qui aurait laissé l'agent orphelin derrière le shim .cmd Windows) ; le process terminé est VÉRIFIÉ mort via tasklist dans le test, pas seulement marqué. (d) <label> accessibles sur les 2 champs texte du déploiement + confirm() dédié sur --force (écrasement de fichiers réels). Vérifié PAR LES FAITS : node --check wiki_app.js, py_compile serve_wiki.py, 5 nouveaux tests HTTP réels (job long annulé, job inconnu, job déjà terminé, route HTTP, filtrage des clés internes _proc/_annule hors sérialisation JSON), suite complète 31 passed sur test_serve_wiki.py.
- **`wiki:accessibilite-onglets`** (2026-07-30) : ACCEPTÉ + APPLIQUÉ (« traite tous ces sujets ») : sémantique ARIA standard posée sur les 9 onglets (scripts/scan_projets.py) : role=tablist sur le conteneur, role=tab + aria-selected + aria-controls sur chaque bouton, role=tabpanel + aria-labelledby + tabindex=0 sur chaque section. La bascule d'aria-selected est câblée dans activer() (docs/wiki_app.js) à côté de la classe CSS actif, qui ne disait jusque-là rien à un lecteur d'écran. Vérifié : node --check, py_compile, rendu régénéré relu.
- **`VSCode3+VSCode4:exclusion-tests-perimee`** (2026-07-30) : ACCEPTÉ + APPLIQUÉ (« traite tous ces sujets ») : l'exclusion « peu de code » de l'arbitrage famille:tests (2026-07-23) était déjà démentie pour ces 2 mêmes projets sur linter/CI (arbitrage famille:linter-ci-decks, 2026-07-28) sans jamais avoir été réappliquée au test. R3 respecté (canal réel différent par projet) : VSCode3 a des tests pytest réels (tests/) -> pytest-cov épinglé (requirements-dev.txt), 1ère mesure 10 % (honnêtement limitée par les tests agent-* qui exercent leurs scripts en subprocess, invisible à --cov). VSCode4 n'a qu'un script autonome sans def test_* pytest -> coverage (pas pytest-cov) épinglé, 1ère mesure 75 % sur scripts/ (generate_deck_ohc.py 96 %). Aucun seuil imposé (mesurer d'abord, modèle VSCode1/VSCode2). Vérifié PAR LES FAITS : suites réellement relancées (32 passed VSCode3, TOUS LES TESTS PASSENT + rendu LibreOffice réel VSCode4), commits scopés poussés (VSCode3 c065dcb, VSCode4 26ccc3c ; churn wiki étranger à chaque cible exclu R2).
- **`reflexion:tendances-wiki`** (2026-07-30) : ACCEPTÉ + APPLIQUÉ (« traite tous ces sujets ») : incrément 5 (Tendances) de docs/reflexions/ameliorations-supervision.md (2026-07-23) implémenté, 7 jours après son écriture. Un snapshot daté des compteurs du bandeau + du niveau d'alerte par projet est écrit à chaque scan dans docs/wiki/history/snapshots.jsonl (scan_projets.py, fonctions snapshot_actuel/ecrire_snapshot/charger_dernier_snapshot/calcule_tendances) ; le bandeau affiche les deltas (▲/▼) sur chaque tuile et les transitions d'alerte par projet (« VSCode2 critique -> majeur », l'exemple même de la réflexion d'origine) vs le scan précédent. Vérifié PAR LES FAITS : 11 tests dédiés (tests/test_wiki_tendances.py, HISTORY_PATH monkeypatché -- jamais le vrai fichier de production dans les tests), suite complète repassée verte.
- **`VSCode`** (2026-07-30) : ACCEPTÉ + APPLIQUÉ (« traite les sujets des actions correctives pour VSCode, VSCode1, VSCode3 et VSCode4 ») : les 5 écarts mesurés pour VSCode traités via evolution-flotte. (1) Doc : README.md racine -- le vrai README vivait dans comop-pptx-prototype/, invisible au scan qui ne regarde que la racine (même classe de défaut de mesure que la leçon scan-detection-mesure du 2026-07-24) ; pointeur, pas une duplication. (2) Cadrage produit : docs/product-brief.md, source = le brief BMAD existant (2026-06-08) -- R1 a révélé que la solution du brief (bibliothèque multi-templates, extraction automatique de charte, 3 onglets) est DÉJÀ largement implémentée (vérifié dans server.js/web/index.html) : le document était en retard sur le code, pas l'inverse. (3) Pratiques+rules : eslint (comop-pptx-prototype/eslint.config.js + pointeur racine pour la détection flotte), 1ère mesure 3 points, + conventions.md. (4) Design : deck-design-review greffée, contrat par slide du support COMOP réel (3 slides), oracle = PowerPoint COM (verify-pptx-integrity.ps1 -RealOpen), pas LibreOffice (canal différent du reste de la flotte, R3). (5) Audit risque_tech : re-vérifié, 3 des 5 findings du 2026-07-23 étaient déjà résolus (package.json réel, CI+tests ajoutés 2026-07-28, sauvegarde archive/ sur remove-template-shape) ; 2 restent honnêtement ouverts (duplication helpers PS, régex XML) et n'ont PAS été refactorés (chantier séparé, hors périmètre d'une correction minimale). Bonus trouvé en chemin (R1) : ppt-designer.md portait un avertissement périmé sur remove-template-shape.ps1 (« régression active ») déjà diagnostiquée et résolue le 2026-07-28 -- corrigé pour ne pas induire un futur agent en erreur. Vérifié par les faits : npm test 13/13 après ajout d'eslint, ruff baseline mesurée (3 points, catch silencieux volontaire). Commit 809c54a poussé (churn wiki du dépôt cible exclu R2).
- **`VSCode1`** (2026-07-30) : CONFIRMÉ SANS NOUVELLE ACTION (« traite les sujets des actions correctives pour VSCode, VSCode1, VSCode3 et VSCode4 ») : VSCode1 n'a qu'un seul écart mesuré -- Audit sécurité (API expose des données nominatives sans authentification). R1 : cadrage réel = ce n'est PAS un manque non traité mais une DÉCISION PRODUIT déjà arbitrée (cible securite:VSCode1-api-pii, 2026-07-25, option C -- Epic 10 complet comme chantier produit, aucun correctif provisoire, échéance 2026-08-08 pour trancher compte local vs SSO/OIDC). Re-vérifié aujourd'hui : cadrage/epics-us.md porte toujours la même décision mot pour mot, l'échéance (2026-08-08) n'est pas encore dépassée. Rien appliqué ici -- refaire le travail aurait été re-deéviner une décision déjà prise plutôt que de vérifier l'état réel (R1).
- **`VSCode3`** (2026-07-30) : ACCEPTÉ + APPLIQUÉ (« traite les sujets des actions correctives pour VSCode, VSCode1, VSCode3 et VSCode4 ») : des 3 écarts mesurés, 2 traités. (1) Doc : README.md racine (commandes réelles, renvoi vers CLAUDE.md/product-brief/wiki). (2) Audit robustesse : re-vérifié, le seul point de fragilité du 2026-07-23 (template chargé sans garde) était DÉJÀ résolu par la garde _exiger_template() du 2026-07-29 -- aucun code écrit, correction minimale = fermeture du finding sur preuve (R1). Reste ouvert, non traité (chantier séparé) : Audit risque_tech (duplication inter-projets de pptx_deck.py, mesurée par la matrice de divergence du hub, pas un oubli). Commit c465ea7 poussé (churn wiki exclu R2).
- **`VSCode4`** (2026-07-30) : ACCEPTE + APPLIQUE (« traite les sujets des actions correctives pour VSCode, VSCode1, VSCode3 et VSCode4 ») : des 6 ecarts mesures, 3 traites. (1) Doc : README.md racine. (2) Pratiques+rules : CI GitHub Actions ajoutee (lint uniquement, informatif -- Exports/ contient le deck source reel, ~25 Mo de contenu RH, jamais versionne -- decision anterieure hors perimetre -- donc le test de generation reel ne peut pas tourner sur un clone frais ; le workflow documente pourquoi plutot que d'echouer silencieusement) + requirements.txt (python-pptx, absent jusqu'ici) + conventions.md. (3) Audit robustesse : 2 des 5 crashs durs du 2026-07-23 corriges -- garde explicite _exiger_source() (template v6 absent) et controle de completude dans charger_media() (medias manquants), SystemExit nommant le fichier/media en cause au lieu d'un crash brut ou d'un KeyError differe. 2 tests de reproduction reelle ajoutes. Reste ouverts, non traites : Test tech./fonct. (le fichier de test unique contient deja 40+ assertions reelles -- le scinder artificiellement pour satisfaire le seuil de comptage aurait ete un gaming de la metrique, pas un vrai travail, donc explicitement ecarte) ; Audit risque_tech (duplication pptx_deck.py, chantier separe) ; le cas doux d'un cadre photo vide qui ne fait pas echouer build() (semi-silencieux, non traite). Verifie par les faits : suite complete rejouee (TOUS LES TESTS PASSENT, 42 assertions, rendu LibreOffice reel 15 pages), ruff 8 points (baseline inchangee), couverture 75 % inchangee. Commit 0ec8e4d pousse (Exports/ et churn wiki exclus R2).
- **`agents:garde-commit-vacante`** (2026-07-30) : ACCEPTE + APPLIQUE (option b de la proposition) : le test assertait not(outils & {'Commit','Git'}) - aucun outil de Claude Code ne porte ces noms, l'assertion etait VIDE et verte par construction alors que 6 des 8 sous-agents portent Bash/PowerShell. Les 8 mandats disent desormais explicitement 'jamais de git add/commit/push/reset', et le test porte sur cette clause en ne verifiant QUE les agents qui ont un shell (plancher a 6, sinon il se re-viderait en silence). Version fausse gardee en commentaire du test. Commit f0c636f.
- **`orchestrateur:regime-office-ecriture`** (2026-07-30) : ACCEPTE + APPLIQUE (a+b+c, arbitrage utilisateur 'implemente propose') : (a) le regime a desormais DEUX criteres cumulatifs, cout ET ecriture - bmad-document-project, bmad-index-docs, bmad-shard-doc et bmad-agent-tech-writer passent en 'propose' (R4 n'interdit pas la depense, il interdit l'auto-application) ; (b) bmad-customize sort du routage tant que le gel skills-jamais-utilisees du 2026-07-27 n'est pas leve explicitement - un gel ne se leve pas tout seul, meme si sa condition (v7) N'EST PAS remplie - verifie a la source le 2026-07-30 : derniere release v6.10.0 du 2026-07-03, aucun tag v7 ; l'entree de veille du 2026-07-29 qui annoncait la sortie se trompait ; (c) nouveau test test_aucune_skill_qui_ecrit_un_fichier_n_est_en_declenchement_d_office + test du gel. Propage a la skill, au catalogue, au mandat de bmad-doc et a l'onglet Dispositif.
- **`agents:types-non-charges-en-session`** (2026-07-30) : ACCEPTE + APPLIQUE (a+b) : (a) regle de degradation en § 2 quinquies - porteur indisponible => skill inline, ou general-purpose avec les interdits RECOPIES (un general-purpose a tous les outils : les garde-fous structurels du porteur redeviennent des consignes), et 'resolution: porteur-indisponible <nom>' au journal ; jamais laisser l'etape echouer. (b) le hook SessionStart annonce les sous-agents apparus depuis son passage precedent (agents_apparus dans le CANON + sync du hub ; premier passage silencieux pour ne pas annoncer l'existant comme neuf ; 5 tests dans test_canon.py). Propagation FAITE aux 6 depots : le test test_action_deterministe_reelle_sync_check du hub asserte que la flotte est synchrone avec le canon, donc un demi-chemin (canon modifie, cibles non synchronisees) laissait la suite rouge - il a fallu aller au bout. Suites cibles rejouees vertes (VSCode2 48 passed, VSCode3 29 passed, hub 105 passed) et un commit SCOPE au seul fichier du canon sur chacun des 5 depots (03b47b2, 95271e2, badc8a9, 29bc2c4, 240753e) : VSCode2 portait 19 fichiers modifies dont 10 applicatifs etrangers a ce chantier, laisses intacts (R2).
- **`orchestrateur:emprunt-routage-bmad-non-mesure`** (2026-07-30) : ACCEPTE + APPLIQUE : emprunt_routage_bmad() mesure dans l'onglet Dispositif 'N/46 skills invoquees, M/8 porteurs lances' depuis state.json (0 token), avec echeance de revue au 2026-08-06 et la regle de decision ecrite (si le compte est encore a zero, c'est le routage qu'il faut instrumenter autrement ou abandonner, pas la table qu'il faut enrichir). Reserve tracee : l'etage 1 compte les TENTATIVES de lancement, pas les succes - le 1/8 actuel vient d'un subagent_type refuse.
- **`veille:contexte-outille`** (2026-07-30) : ADOPTE + APPLIQUE : regle 'discipline de contexte/tokens documentee' entree au referentiel criteres-pratiques.md et OUTILLEE dans scan_projets.py (discipline_tokens : exige un TITRE de section, pas une occurrence du mot 'token' - les CLAUDE.md en parlent en passant, un compteur d'occurrences aurait rendu 6/6 vert et menti ; 5 tests). Entree veille passee en 'adopte'. La trouvaille se trompait sur l'etat reel : elle annoncait la discipline ecrite sur VSCode1/VSCode3 seulement, la mesure dit 5/6 - le SEUL manque est le hub lui-meme. Action corrective restante a arbitrer : ecrire la section dans le CLAUDE.md de VScode5 (fichier de regles de l'utilisateur, non modifie sans son accord).
- **`risque-technique:VSCode-dedup-helpers-ps`** (2026-07-30) : ACCEPTE + APPLIQUE (bouton Valider du wiki, session non interactive, option A) : sur le finding risque_technique de l'audit VSCode (« TOUJOURS OUVERT : duplication de helpers Get-ShapeName / Get-GraphicType / New-TempDirectory copies entre 2 scripts »), remediation via evolution-flotte. Cadrage reel (R1) : Get-ShapeName et Get-GraphicType etaient byte-pour-byte identiques dans detect-template-zones.ps1 et remove-template-shape.ps1 ; New-TempDirectory existait dans detect et etait re-code inline dans remove (prefixe 'remove-shape-'). L'autre finding ouvert (regex XML, generate-comop.ps1) laisse INTACT — design assume du canal COMOP, pas une regression, hors perimetre d'une correction minimale (option A). Fix (R3, canal PowerShell) : nouveau module src/pptx-xml-helpers.ps1 dot-source par les 2 scripts via $PSScriptRoot (resolu sous l'invocation reelle -File du serveur) ; New-TempDirectory prend un -Prefix pour preserver les reperes distincts (zones-detect- / remove-shape-) ; Get-ShapePosition/Get-TextApercu (mono-usage dans detect) laisses locaux = correction minimale, pas une refonte. Verifie PAR LES FAITS (pas une lecture) : (1) revue fraiche en contexte frais (sous-agent, diff seul) = aucun defaut bloquant (prefixes preserves, ordre de definition OK, $PSScriptRoot non vide sous -File, aucun helper orphelin/mort, perimetre propre) ; (2) test technique = parser PowerShell OK sur les 3 fichiers + npm test 13/13 ; (3) test fonctionnel avant/apres sur le template reel comop-template.pptx = detect rend 36 zones structurellement IDENTIQUES (hors timestamp), remove d'une image rend 35 zones + sauvegarde archive/ creee + PPTX mute ouvrable en zip OOXML (126 entrees), forme supprimee absente. Commit scope 9114ec9 pousse sur origin/main (git diff --cached --name-only = les 3 fichiers src/ UNIQUEMENT ; churn supervision routing-hints/state/usage + docs/wiki du depot cible exclu R2). Audit VSCode risque_technique : finding duplication marque CORRIGE ; la dimension reste 'moyen' A DESSEIN (le finding regex XML reste ouvert, dette assumee du canal).
- **`veille:bmad-uv-run`** (2026-07-30) : ACCEPTE + APPLIQUE (« install uv ») : uv 0.11.32 installe sur le poste via winget (canal officiel astral-sh.uv, hachage verifie ; pas de script distant pipe dans un shell). Motif REEL, independant de la v7 qui n existe pas : 22 fichiers du BMAD v6.10.0 DEJA installe invoquent 'uv run' en forme primaire, avec un repli python3 documente mais non garanti si un agent suit le texte des skills a la lettre. Verifie par les faits : uv --version = 0.11.32, 'uv run python -c import tomllib' OK sous Python 3.14.5, et le vrai resolveur du dispositif (_bmad/scripts/resolve_config.py) execute par uv rend son JSON de configuration. Portee : POSTE de travail, une seule fois pour les 6 depots - hors git, additif, rien a reverter, aucun commit de code requis.
- **`etudes:lot1-gains-purs`** (2026-07-30) : ACCEPTE + APPLIQUE : (a) refresh_local_scans parallelise -- A/B mesure sur la vraie flotte 6,8 s -> 2,2 s, soit 4,6 s (68 %), resultats identiques ; l'etude estimait 12-20 s, c'etait faux et je retiens le chiffre mesure. (b) encart de job pre-rempli : il restait vide 5 a 20 s sur un job LLM. (c) discipline de lecture completee -- 4 fichiers generes de 78-94 Ko ajoutes a la liste 'jamais en entier', et regle de lecture par la FIN pour runs.jsonl (un Read entier coute ~109 000 tokens, mesure). 6 tests. Commit 939bdd5.
- **`etudes:lot2-instrumentation`** (2026-07-30) : ACCEPTE + APPLIQUE : (a) scripts/mesure_tokens.py -- PREMIERE mesure de tokens du projet : 74 241 819 facturables sur 119 transcripts et 10 299 messages, ventiles par jour et par modele, en 15 s. Volontairement hors du hook de session pour ne pas l'alourdir. Invariant teste : la sortie ne contient QUE des entiers et des dates, jamais un contenu de message. (b) journal disque des jobs du wiki (.claude/supervision/jobs.jsonl) : /api/jobs n'existait qu'en memoire. La sortie de l'agent n'y est jamais ecrite (teste). (c) git_etat : dette non commitee et branches mesurees sur les 6 depots -- le hub ne voyait que lui-meme alors que R2 est sa lecon la plus chere. Ferme 2 criteres du referentiel, dont trunk-based qui etait VERT sans que personne ne le sache (6/6 a une seule branche). 19 tests. Commit d9ed306.
- **`VSCode4:tests-non-decouvrables`** (2026-07-30) : ACCEPTE + APPLIQUE (playbook evolution-flotte) : les 291 lignes et 42 assertions de scripts/test_generate_deck_ohc.py passaient deja mais vivaient dans un main() sans aucun def test_ -- invisibles de pytest et de la couverture. Portees telles quelles en 3 fichiers pytest (structure / qualite / rendu) + conftest a build partage, sur le modele et le canal de VSCode3. L'ancien script devient un lanceur qui delegue a pytest, avec son propre --basetemp (jonction morte du %TEMP% de cette machine). pytest ajoute a requirements-dev.txt. 25 tests verts dont le rendu LibreOffice REEL (15 pages). Mesure apres coup : test_technique et test_fonctionnel passent orange -> ok. C'ETAIT LE SEUL COUPLE DE PASTILLES NON VERTES DE LA FLOTTE. Commit scope 966e693 sur VSCode4 ; wiki de ce depot et Exports/ laisses dehors (R2).
- **`agents:emprunt-routage-bmad`** (2026-07-30) : REPORTE AU 2026-08-06 (arbitrage utilisateur) : les 8 sous-agents crees ce jour sont a 0/46 skills empruntees et 2/8 porteurs lances. L'etude de propagation recommandait de ne pas les propager a la flotte (VSCode2 prouve que le routage BMAD marche sans eux, en inline) et notait qu'ils multiplieraient par 5 le finding garde-commit. Decision de TENIR le rendez-vous que le hub s'est lui-meme donne : une semaine d'usage reel est le seul juge honnete, ils ont quelques heures. A cette date, si le compte est encore a zero, c'est le routage qu'il faut instrumenter autrement ou abandonner -- pas la table qu'il faut enrichir.
- **`securite:VSCode1-api-pii`** (2026-07-30) : ACCEPTÉ + APPLIQUÉ (bouton « Valider » du wiki, session non interactive, option A) : REVIREMENT ASSUMÉ de l'arbitrage du 2026-07-25 (qui avait retenu l'option C — Epic 10 complet, PAS de barrière provisoire, options A Basic Auth et B jeton écartées). L'utilisateur, arbitre (R4), revient sur ce choix et retient désormais l'option A : poser une barrière HTTP Basic Auth INTÉRIMAIRE sur la surface animateur/PII pour ne pas laisser l'API nominative (nom/prénom/email) ouverte jusqu'à la livraison de l'Epic 10. Ce n'est PAS une auto-proposition de barrière par l'agent (interdit hérité du 2026-07-29 respecté) : c'est l'application d'un choix utilisateur explicite. Cadrage réel d'abord (R1) sur le dépôt cible : app Express, API /api/* sans aucune auth, parcours répondant qui DOIT rester ouvert (US10.5 — questionnaire par lien de session, sans compte). Fix (R3, canal Express) : nouveau middleware app/src/auth.js — ENV-GATED (actif seulement si AUTH_USER ET AUTH_PASS posés ; sinon no-op, comportement dev/CI strictement inchangé, avertissement au démarrage), FAIL-CLOSED (tout /api + pages animateur admin/pilotage/resultats protégés par défaut ; un oubli ferme, ne fuit pas), avec une liste blanche EXPLICITE des 11 routes + pages du parcours répondant (préserve US10.5). Comparaison des identifiants à temps constant (SHA-256 + crypto.timingSafeEqual, pas de court-circuit). Câblé en tête de chaîne dans server.js (avant static/json) pour couvrir aussi les pages animateur (l'invite Basic du navigateur met les creds en cache pour les fetch de même origine). VÉRIFIÉ PAR LES FAITS (pas une lecture) : (1) test technique — node --check sur les 3 fichiers + eslint propre ; un bug RÉEL trouvé et corrigé par le test fonctionnel (en-tête WWW-Authenticate avec tiret cadratin → ERR_INVALID_CHAR → 500 ; passé en ASCII strict) ; (2) test fonctionnel — scripts/test-auth.js (dans npm test) démarre le VRAI serveur en HTTP réel et prouve : GET /api/sessions (collection admin) et /admin.html = 401 sans creds, 200 avec les bons, 401 avec de mauvais ; parcours répondant (env/roles/departements/equipes/texte-intro + repondre.html) = 200 sans creds ; et sans creds d'env la même route reste 200 (contrôle : comportement inchangé). npm test COMPLET vert (dont test-auth) ; (3) revue en contexte frais (sous-agent sonnet, diff seul + exigences) = RAS, le relecteur a rejoué le test réel et vérifié la classification des 6 pages et chaque regex de l'allowlist un par un. Commit scopé 4c2570a poussé sur origin/main (git diff --cached --name-only = 7 fichiers du périmètre : auth.js, test-auth.js, server.js, package.json, .env.example, README.md, cadrage/epics-us.md ; churn préexistant docs/wiki + dev-server.* exclu R2). HONNÊTETÉ (R5, interdit du 2026-07-29 respecté) : le finding sécurité et l'Epic 10 restent OUVERTS — c'est une mesure de réduction de risque, PAS l'authentification produit (aucun compte, pas de rôle lecture-seule US10.3, pas de cycle de vie US10.4/US10.6, protection env-gated donc off par défaut). La dimension sécurité de l'audit VSCode1 reste « moyen » À DESSEIN ; échéance de conception de l'Epic 10 inchangée (2026-08-08). Décision de révision tracée aussi dans cadrage/epics-us.md de la cible. [2026-08-31] Categories requalifiees sur le vocabulaire du dispositif (anciennement securite, audit, pii, auth — hors vocabulaire, donc sans effet sur la cloture des findings : le hook SessionStart le signalait a chaque demarrage). Le vocabulaire ne porte pas de categorie securite ; l'objet de l'arbitrage est inchange.
- **`bmad-customize`** (2026-07-31) : ACCEPTE + APPLIQUE (« leve le gel sur la v7 et restons en V6 ») : LEVEE du gel pose par l'arbitrage skills-jamais-utilisees du 2026-07-27 (« aucune customisation BMAD jusqu'a la v7 »). Motif de la levee : la condition d'attente ne se realise pas — v6.10.0 est toujours la derniere release (verifie le 2026-07-30 sur l'API GitHub releases/latest, aucun tag v7*), et l'entree de veille du 2026-07-29 qui annoncait la v7 se trompait. Un gel conditionne a un evenement qui ne vient pas est un gel definitif qui ne dit pas son nom. Decision utilisateur : rester en v6 ET customiser des maintenant. Applique : bmad-customize entre dans la table de routage de agent-orchestrator/SKILL.md (§ 2 quinquies) en regime PROPOSE — elle ecrit un fichier reel (_bmad/custom/<skill>.toml), donc R4 s'applique en entier : l'orchestrateur annonce et attend le feu vert. Le paragraphe « Gelee par arbitrage » est remplace par le constat de la levee, place AVANT le titre « Jamais routees » (qui aspire tous les backticks jusqu'a la fin du bloc — sinon la skill etait routee et non routee a la fois). Verrou de test retourne : test_une_skill_gelee_par_arbitrage_n_est_pas_routee devient test_la_levee_du_gel_bmad_customize_est_tracee (routee + regime propose + tracabilite exigee dans ce fichier), et un nouveau test_aucune_skill_a_la_fois_routee_et_non_routee garde l'incoherence qui vient d'etre commise. Ce qui n'est PAS decide ici : la migration vers la v7 le jour ou elle sortira — les overrides ecrits en v6 devront etre re-verifies a ce moment-la. Contexte declencheur : l'etude d'une party-mode elargie, dont le canal d'ecriture officiel est precisement bmad-customize.
- **`VSCode1`** (2026-07-31) : ACCEPTE + APPLIQUE (« traite vscode1 ») : canal des secrets separe du canal de configuration. Constat d'origine requalifie par le cadrage reel (R1) — la remontee initiale « .env.prod versionne = fuite de secrets » etait SURESTIMEE : les 3 fichiers app/.env.dev|.preprod|.prod ne portent que APP_ENV, PORT, DB_PATH, aucune valeur sensible, et src/auth.js n'a aucun mot de passe par defaut (barriere env-gated, fail-closed, comparaison a temps constant). Le risque reel est different et il est documentaire : app/.env.example se contredisait — « Ne PAS committer de vrai .env » en en-tete, « Poser de VRAIS identifiants uniquement dans le .env de PROD » plus bas — alors que .env.prod EST suivi par git et qu'aucune regle .gitignore ne couvre .env*. Un exploitant qui suivait la consigne commitait AUTH_PASS. Applique, scope au strict necessaire : (1) app/.gitignore ignore .env*.local, canal des secrets ; (2) les scripts start:dev|preprod|prod chargent en second --env-file-if-exists=.env.<env>.local (Node >= 20.12, compatible avec engines node >= 22 declare ; verifie : demarre sans le fichier, le secret surcharge quand il existe, git l'ignore) ; (3) app/.env.example reecrit — deux canaux explicites, contradiction levee, et CHROME_PATH/CAPTURES_OUT documentees (elles sont lues par scripts/capture-screenshots.js alors qu'un commentaire affirmait a tort que CHROME_PATH avait ete retire le 2026-07-21) ; (4) garde-fou executable app/scripts/test-env-sans-secret.js, chaine dans npm test au canal du projet (scripts node enchaines, pas de framework ajoute — R3) : il echoue si une variable au nom sensible est posee dans un .env suivi par git, et n'affiche jamais la valeur. Le garde-fou a ete VU ROUGE avant d'etre cru (AUTH_PASS injecte dans .env.dev -> exit 1), puis restaure. Les 3 .env restent versionnes : ce sont du parametrage d'environnement, pas des secrets. NON fait, et assume : aucune reecriture d'historique git (inutile, rien de sensible n'y est).
- **`bmad-party-mode`** (2026-07-31) : ACCEPTE + APPLIQUE (« Creer la party puis documenter le reel ») : party elargie ECRITE dans _bmad/custom/bmad-party-mode.toml (override TEAM, versionne), via bmad-customize — premiere customisation BMAD depuis la levee du gel du meme jour. PREREQUIS TECHNIQUE d'abord, sans quoi tout le reste etait decoratif : bmad-party-mode NE DEMARRAIT PAS sur ce poste (console cp1252). resolve_party.py:_run_json decodait en cp1252 la sortie UTF-8 de resolve_customization.py -> UnicodeDecodeError dans le thread lecteur, puis AttributeError ligne 52 HORS du try ; une fois ce point corrige, resolve_config.py:174 replantait a l'ecriture (emoji sur stdout cp1252) et la salle se resolvait a 0 membre. Corrige en un seul point (encoding utf-8 + PYTHONIOENCODING passe a l'enfant + garde explicite sur out.stdout None, qui retablit le contrat que la fonction annonce). Les deux fichiers sont des fichiers core BMAD a empreinte SHA : fork assume et trace dans .claude/patches/bmad-party-mode-resolve_party.md, garde par tests/test_bmad_party_mode_resolve.py (canari source + execution reelle, VU ROUGE par injection de faute). Le canari a d'ailleurs ete corrige apres avoir ete pris en defaut : il cherchait ses marqueurs dans le fichier entier et restait vert malgre l'ecrasement, car encoding="utf-8" apparait aussi dans le reconfigure() de main() — il cible desormais le corps de _run_json seul. CASTING : 18 roles maison (4 gouvernance : supervision/veille/intendance/qualite ; 3 dev differencies par COUCHE de fichiers, pas par personnalite ; 6 relais de projet ; 5 transverses : deck/conformite/deploiement/doc/connecteurs) et 5 salles de 5 voix maximum, plafond de l'arbitrage veille:agent-teams. Le merge etant keye, les 9 personas et 2 groupes livres par la skill SURVIVENT : vivier reel de 33 voix, 7 salles. default_party volontairement NON surcharge (la salle d'entree reste les 6 agents BMAD ; ouvrir une salle a 5 voix a chaque session se paierait). Verifie par execution : les 7 salles se resolvent, aucun membre orphelin. DOCUMENTE : onglet Tutoriel du wiki — 5 concepts (party, role, salle, relais, mode) + un schema DERIVE des TOML reels (jamais recopie, meme invariant que l'onglet Dispositif), verrouille par tests/test_wiki_party.py. Deux defauts trouves en regardant le rendu reel et corriges : les agents installes manquaient au collectif (Sally s'affichait « non resolu » alors que le vrai resolveur la trouve), et les scenes livrees par BMAD (~1000 caracteres d'anglais) ecrasaient les notres — tronquees a l'affichage. Corrige au passage une carte du tutoriel devenue MENSONGERE apres la levee du gel (« statu quo decide jusqu'a la v7 »). NON fait : aucune table ronde n'a encore ete TENUE — la resolution est prouvee, l'usage reste a experimenter.
- **`tests:gardes-vacants`** (2026-07-31) : ACCEPTE + APPLIQUE (« applique les corrections identifiees ») : les 2 tests vacants prouves par mutation sont corriges, et VUS ROUGES avant d'etre crus. (1) test_la_levee_du_gel_bmad_customize_est_tracee cherchait « bmad-customize » et « 2026-07-31 » comme deux sous-chaines INDEPENDANTES du fichier entier — deux entrees sans rapport le satisfaisaient, il restait vert apres suppression de l'arbitrage qu'il exige. Il interroge desormais l'OBJET CHARGE : meme entree, cible ET date, plus le sens de la decision. (2) test_les_salles_respectent_le_plafond_de_cinq_voix comptait len(members or []) : une salle open-cast, sans cle members, comptait 0 et n'etait jamais controlee — le plafond ne gardait que les salles deja bornees par construction. Les deux cas sont separes, et l'open-cast doit annoncer sa nature dans sa scene. REGLE GENERALE retenue de la proposition : assertion sur objet charge, jamais sur sous-chaine de fichier. NON FAIT : le vacant de test_serve_wiki.py:412-483 (le test reecrit la condition de production) reste ouvert — il touche un autre sujet que cette seance, a traiter a part.
- **`bmad-forge-idea`** (2026-07-31) : ACCEPTE + APPLIQUE : la correction passe de l'INSTANCE a la CLASSE. resolve_personas.py etait un jumeau exact de resolve_party.py, non patche et non garde : patche a l'identique. Surtout, la cause racine est corrigee cote PRODUCTEUR — _bmad/scripts/resolve_config.py ecrivait ensure_ascii=False sur un stdout non reconfigure, ce qui faisait sortir 4 skills sur 46 en exit 1 (bmad-forge-idea, bmad-retrospective, bmad-help, bmad-advanced-elicitation). BMAD avait DEJA resolu ce probleme dans resolve_customization.py (helper write_json_stdout, commentaire « so Windows cp1252 stdout can carry emoji icons ») et avait oublie de l'appliquer ici : le correctif aligne les deux. Verifie par execution : les 3 scripts sortent en exit 0 avec du JSON parsable. GARDE DE CLASSE ajoute (TestCanariDeClasse) : il parcourt tous les consommateurs et producteurs connus, et ECHOUE si un script porte la signature fautive sans figurer dans la liste — verifie par injection d'un faux jumeau. Fork elargi et trace dans .claude/patches/.
- **`dispositif:presence-vs-fonctionnement`** (2026-07-31) : ACCEPTE + APPLIQUE : « compter n'est pas essayer ». La classe TestSmokeDesResolveurs EXECUTE les points d'entree des 4 resolveurs et exige exit 0 + JSON parsable — ce qu'aucun compteur de presence ne peut voir. Le constat est inscrit en regle R6 du CLAUDE.md (« l'etage 1 mesure la presence, jamais le fonctionnement »). NON FAIT, et assume : le scan ne lance pas encore le point d'entree de CHAQUE skill a chaque passage — ce serait couteux en temps de scan pour 341 skills ; le smoke couvre les resolveurs, la ou le bug s'est reellement produit.
- **`etudes:faits-verifiables-non-verifies`** (2026-07-31) : ACCEPTE + APPLIQUE : regle R6 ajoutee au CLAUDE.md — tout chiffre, nom de script, compte d'artefacts ou version publiee s'ecrit avec la commande qui l'a produit, sinon il est marque « estimation non mesuree » ; et si une verification est executable, elle passe AVANT la redaction. La regle porte les chiffres du finding (6 corrections en 48 h, evolution-flotte 30 runs / 14 reprises) pour qu'elle ne se lise pas comme un conseil de style. Capitalise aussi en memoire (feedback-executer-avant-de-rediger). NON FAIT : la proposition « revue-increment rejoue 3 affirmations chiffrees par echantillon » n'est pas outillee — elle demanderait de decider comment echantillonner, ce qui merite son propre arbitrage.
- **`veille:trouvailles-dormantes`** (2026-07-31) : PARTIELLEMENT APPLIQUE : le VOLET DISPOSITIF est fait — le wiki affiche desormais l'age de la doyenne trouvaille non arbitree a cote de la fraicheur de la veille, avec une alerte au-dela de 7 jours (mesure au rendu : « 2 j / doyenne 8 j ». Un dispositif qui parait tourner alors que ses propositions pourrissent est precisement ce que le finding denonce. Le VOLET DECISION n'est PAS applique et ne peut pas l'etre par l'agent : adopter ou ecarter chacune des 4 trouvailles en attente est un arbitrage utilisateur (R4). Les 4 sont presentees pour decision.
- **`veille:awesome-claude-code`** (2026-07-31) : ADOPTE + APPLIQUE : index communautaire inscrit comme source recurrente de la veille, dans veille-agentic/SKILL.md section « Sources a surveiller (publiques) ». Verifie a la source avant adoption (R6) : 51 394 etoiles, pousse le jour meme, CONTRIBUTING + CODE_OF_CONDUCT + SECURITY, et index GENERE programmatiquement depuis des entrees structurees — curation reelle, pas un dump de liens. La puce ajoutee porte sa propre reserve : un index signale ce qui EXISTE, il ne dit ni si c'est vivant ni si ca vaut pour cette flotte, donc chaque entree retenue se verifie a la source avant d'etre proposee. Cout : une edition de fichier, 0 run facture.
- **`veille:multi-agent-observability`** (2026-07-31) : INSTRUIT, PAS ADOPTE (statut etudie) : le repo est dormant — 1 501 etoiles mais dernier push le 2026-02-08, ~5,7 mois. La note de pertinence de la veille etait FAUSSE sur un point verifiable : pas de swim lanes par agent, mais une timeline unifiee. Pile Bun/SQLite/Vue ecartee (un process persistant contredit un hub qui regenere un wiki statique a 0 token). L'ECART REEL est ailleurs et il est mesure : ils captent 12 types d'evenements, nous UN SEUL (log_usage.py, 42 lignes). Deux pistes a cout borne restent EN ATTENTE D'ARBITRAGE : capturer la fin de sous-agent (duree, issue) et un panneau « runs en cours ». Non applique — ce sont des ecritures sur le dispositif.
- **`veille:hve-core`** (2026-07-31) : INSTRUIT, MIGRATION ECARTEE (statut etudie) : la skill PPT de hve-core vit sous .github/skills/EXPERIMENTAL/, non stabilisee par Microsoft lui-meme. Migrer VSCode3/VSCode4 vers un pipeline YAML serait une refonte sans point de douleur identifie, contre R1 (correction minimale > refonte) : les deux generateurs Python sont fonctionnels et deja arbitres le 2026-07-23. On y perdrait ajuster_police (adaptation dynamique au texte reel, indispensable sur du contenu client variable) et les 22 patterns de deck-design-library, sans preuve que leur validateur capte les 7 defauts que pptx-verify chasse par RENDU REEL. Sans objet pour VSCode (COMOP Node.js — R3). Une seule piste retenue, en attente d'arbitrage : importer le mode dry-run (valider sans construire) dans pptx-deck.
- **`veille:dev-browser`** (2026-07-31) : INSTRUIT, ADOPTION CIBLEE EN ATTENTE (statut etudie) : SawyerHood/dev-browser, MIT, 6 490 etoiles, pousse le 2026-07-15. Lecture correcte de son architecture : le sandbox QuickJS WASM restreint le SCRIPT ecrit par l'agent, pas le navigateur — Chrome reste un process normal qui atteint localhost. SANS OBJET pour VSCode1, qui pilote deja un vrai Chrome sur localhost:3000 (app/scripts/capture-screenshots.js). OBJET REEL pour VSCode2 : grep confirme aucun playwright/selenium/puppeteer dans tests/ — app FastAPI verifiee par pytest seul, aucun controle de rendu. Non applique : installer un outil sur un depot de la flotte est une decision utilisateur, et cela ajouterait une dependance Node a un projet Python pur (a isoler en outillage agent, jamais dans requirements.txt).
- **`veille:fork-revue-fraiche`** (2026-08-31) : ACCEPTE + APPLIQUE (commande « applique tout cela ») : depuis Claude Code v2.1.232 le sous-agent fork est actif par defaut et HERITE du contexte de l'appelant, ce qui vide de son sens l'etape de revue en contexte frais sans que rien ne le signale. L'etape revue-fraiche des playbooks evolution-flotte et export-ppt-verifie exige desormais un sous-agent standard isole (jamais fork ni /subtask) ; le critere est inscrit au referentiel criteres-pratiques.md. La variable d'environnement CLAUDE_CODE_FORK_SUBAGENT=0 n'a PAS ete posee : activer ou desactiver une capacite du harness est une decision separee, proposee a l'utilisateur.
- **`veille:dynamic-workflows`** (2026-08-31) : ACCEPTE + APPLIQUE (commande « applique tout cela ») : critere de bascule fan-out manuel vs dynamic workflow ecrit au §2 ter de agent-orchestrator/SKILL.md, en miroir du paragraphe agent teams — bascule envisagee au-dela d'une poignee de sous-agents, quand les resultats doivent se verifier entre eux, ou quand la campagne sera rejouee. Garde-fou inscrit : opt-in explicite de l'utilisateur obligatoire, jamais sur initiative de l'orchestrateur (l'outil peut lancer des dizaines d'agents).
- **`veille:cache-ttl-subagents`** (2026-08-31) : ACCEPTE PARTIELLEMENT + APPLIQUE (commande « applique tout cela ») : la regle d'analyse est adoptee et inscrite au referentiel criteres-pratiques.md (critere mesurable a froid par grep sur .claude/agents/*.md). L'ACTIVATION de experimental.cacheTtl sur les cinq porteurs BMAD n'est PAS faite : l'action corrective de la veille la conditionne elle-meme a une mesure /cost reelle, qui n'a pas ete produite. Activer une capacite experimentale sans sa mesure serait exactement le geste que le referentiel reproche.
- **`veille:cctop`** (2026-08-31) : ECARTE (commande « applique tout cela », arbitrage global) : l'entree de veille ne porte ni regle_proposee ni action_corrective — c'est un moniteur temps reel optionnel, cote operateur. Un process persistant de surveillance est etranger a la philosophie du hub (etage 1 deterministe a 0 token, lance par hook). Reste installable a la main si l'utilisateur le souhaite ; ne sera pas re-propose.
- **`pdf:retrait-citation-vscode2`** (2026-08-31) : ACCEPTE + APPLIQUE (« conserve les choix appliques du filet ombre ») : la chaine PDF de VSCode2 GARDE son retrait de citation de 10 pt (3,53 mm) a l interieur de l encadre. Le fond de l encadre reste sur la grille a 20,00 mm ; seul le texte est en retrait, ce qui laisse la place au filet lateral ambre repris du .docx modele client. Le verificateur pdf_verify.py de la skill pdf-quality signalait ce cas comme bloquant (bords_gauche_multiples) : il porte desormais une option --retrait-citation-mm qui l accepte QUAND IL EST DECLARE, sans cesser de detecter un vrai desalignement (verifie : le PDF fautif rend toujours 9 bloquants avec l option active). Test de non-regression ajoute et vu ROUGE avec l option neutralisee. Aucun changement dans VSCode2 : le choix de design y etait deja.
- **`tests:verdict-non-reproductible`** (2026-08-31) : ACCEPTE + APPLIQUE (revue du hub, tranche « verdict de la suite reproductible ») : (1) pyproject.toml recoit [tool.pytest.ini_options] testpaths=[tests] + norecursedirs — la collecte racine passe de 444 a 440 et egale desormais celle de tests/, donc pytest ne collecte plus les 46 tests embarques dans export/ et ne pollue plus le kit qu'il verifie ; (2) export_agentic.verifier() detecte les ORPHELINS (fichier present dans export/ hors manifeste) et sort 1 — il annoncait 47/47 a jour, exit 0, pendant que son propre test etait rouge ; (3) les 14 chemins de bytecode d'export/ sont supprimes ; (4) test_action_deterministe_reelle_sync_check n'echoue plus que sur une DERIVE (corps), une derive d'en-tete etant signalee sans faire echouer — la suite du hub ne depend plus de l'etat cosmetique de 4 depots exterieurs ; (5) course ConnectionAborted drainee cote serve_wiki, stabilite prouvee sur 10 executions consecutives. Verdict avant : 396 passes / 2 echecs. Apres : 457 passes / 0 echec.
- **`veille:decision-non-reinjectee`** (2026-08-31) : ACCEPTE + APPLIQUE (revue du hub, tranche « verite des chiffres ») : point_du_jour.trouvailles_en_attente() croise desormais veille.json avec arbitrages.json (fonction _veille_arbitree, meme principe que finding_arbitre()), au lieu de filtrer sur le seul champ statut. Une exception mesuree a ete ajoutee apres coup : un arbitrage dont le VERDICT (tete de la decision, avant le premier « : ») dit EN ATTENTE ne ferme pas la trouvaille — sans elle, le correctif fermait veille:dev-browser, dont la decision dit « ADOPTION CIBLEE EN ATTENTE », c'est-a-dire enterrait la seule attente reelle par l'autre bout. Marqueur cherche dans la tete seulement : le CORPS de 2 decisions conclusives sur 3 contient « en attente » a propos d'autre chose. Hook reel : 4 trouvailles annoncees -> 2 (doyenne 33 j au lieu de 39). RESTE A ARBITRER, hors correctif : la trouvaille BMAD v7 reste comptee en attente parce qu'aucun arbitrage a cible veille:<slug> ne la couvre — sa decision a ete tracee sous bmad-customize.
- **`referentiel:deux-sources-qui-se-contredisent`** (2026-08-31) : ACCEPTE + APPLIQUE (revue du hub, tranche « verite des chiffres ») : (1) criteres-pratiques.md pose explicitement en tete les DEUX AXES ORTHOGONAUX qui faisaient diverger les deux sources — ce document repond « ce critere est-il OUTILLE ? » (✅/🔍/⬜), la table CRAFT_PRATIQUES de scan_projets.py repond « quel est l'ETAT de la flotte ? » (ok/moyen/absent) — et pose la regle : un ✅ nomme la fonction qui le mesure, sans quoi il n'est pas gagne ; (2) le critere trunk-based est realigne des deux cotes sur la mesure reelle : CRAFT_PRATIQUES disait « Non outille — mesurable via git branch » et « ⬜ pas encore mesure » alors que git_etat() (scan_projets.py:159, comptage git branch l. 184-189, seuil DORA < 3 au rendu) le mesure dans le MEME fichier depuis le 2026-07-30 ; (3) l'entree « detection trunk-based » est retiree des « ecarts a outiller », ou le document se contredisait a deux lignes d'intervalle. Plus aucun critere de la table n'est marque « non outille ». Verifie au rendu : page servie HTTP 200, git_etat() present dans docs/wiki.html.
- **`flotte:canon-ecrit-jamais-commite`** (2026-09-01) : ACCEPTE + APPLIQUE (arbitrage utilisateur du 2026-09-01, « lance tous ces chantiers ») : (a) le playbook evolution-flotte porte desormais une ETAPE TERMINALE OBLIGATOIRE a son etape commit-scope — une ecriture sur un depot tiers n'est PAS terminee tant qu'elle n'est pas soit commitee dans CE depot, soit couverte par une ligne d'arbitrage a la cible flotte:<projet>-commit-dispositif nommant un proprietaire et une echeance ; message normalise « dispositif: <quoi> depuis le hub » pour toute propagation du dispositif. (b) git_etat() mesure et le tableau de pilotage affiche l'AGE du doyen non commite a cote du compte (« 20 non commites » ne distingue pas une seance en cours d'une dette de 39 j ; « 20 · doyen 39 j » tranche d'un coup d'oeil) — verifie dans le HTML SERVI le 2026-09-01, la jauge rend bien « doyen 39 j ». (c) Le moratoire « tant que (a) n'est pas en place, le hub ne propage plus » est LEVE par construction : (a) est en place, donc la propagation redevient autorisee — a la condition que son etape terminale soit reellement jouee, ce que le playbook impose maintenant. RESTE OUVERT ET NON COUVERT PAR CET ARBITRAGE : les 80 fichiers non commites deja presents chez les 5 cibles (VSCode 7, VSCode1 20, VSCode2 13, VSCode3 20, VSCode4 20) sont une dette CONSTITUEE avant la regle ; la regle empeche la prochaine, elle ne solde pas les anciennes.
- **`flotte:agent-orchestrator-socle-vs-local`** (2026-09-01) : ACCEPTE + APPLIQUE PARTIELLEMENT (arbitrage utilisateur du 2026-09-01, « lance tous ces chantiers ») : (a) APPLIQUEE — le SKILL.md de chaque cible est coupe en un SOCLE regenere depuis le hub et un chapitre « Portee sur ce projet » jamais reecrit, via .claude/dispositif/propager_socle.py, qui REFUSE d'ecraser une cible dont le chapitre local est introuvable. Il a refuse 4 cibles sur 5 au premier passage : leur texte local etait tisse dans les sections du socle et a du etre lu puis deplace a la main. Preserve et verifie ligne a ligne : journal en deux temps (VSCode), flotte OpenCode/Beads (VSCode1), table des matrices swot/priority (VSCode2), statut colonne vertebrale (VSCode3), « repris mais pas rejoue » (VSCode4). (b) APPLIQUEE — export_agentic.py --check-flotte compare le kit INSTALLE chez chaque projet a celui publie, et distingue « socle-a-jour + local preserve » d'une derive reelle. Premiere mesure : 102 ecarts et 23 absents sur 6 depots x 47 fichiers ; apres propagation, 97. Lecture seule garantie par test de mtime. (d) APPLIQUEE — chaque copie porte « socle : <hash court du hub> du <date> », et la ligne est LUE par --check-flotte, pas seulement ecrite. (c) NON TRANCHEE, REMONTEE A L'UTILISATEUR — la proposition demandait de supprimer ou marquer OBSOLETE VSCode2/export/. Trois faits mesures par la session vscode2-3d contredisent la premisse du finding : export/ y est un bundle de distribution SUIVI (13 fichiers) et documente par son README, maintenu il y a 5 commits (0f8ca53) ; seul export/agent-orchestrator/SKILL.md a decroche (un seul commit, 0b414f9). Ce n'est donc pas un residu fige mais un livrable vivant dont un fichier a decroche, et le geste adapte est probablement une re-synchronisation (precedent 2efa835), pas une suppression. Decision laissee a l'utilisateur : un arbitrage sur le socle de la skill ne vaut pas arbitrage sur le sort du bundle. CORRECTION DE FAIT AU PASSAGE : le finding annoncait VSCode2 a 154 lignes sans aucune section de capacite ; la lecture reelle du 2026-09-01 en a trouve 526 avec 5 sections sur 6. La donnee du diagnostic avait vieilli. Les 5 depots sont commites chez eux (1 seul fichier stage a chaque fois), aucun push.
- **`flotte:vscode2-export-bundle`** (2026-09-01) : SUJET ANNULE PAR L'UTILISATEUR (2026-09-01, « annule le sujet VSCode2/export ») — a ne plus representer. C'est la proposition (c) du finding flotte:agent-orchestrator-socle-vs-local, la seule des quatre qui restait ouverte : elle demandait de supprimer ou de marquer OBSOLETE VSCode2/export/agent-orchestrator/SKILL.md. Elle reposait sur une lecture que la mesure a contredite. Trois faits etablis par la session vscode2-3d dans son propre depot : export/ y est un bundle de distribution SUIVI (13 fichiers) et documente par son README ; il est MAINTENU (commit 0f8ca53, 5 commits avant HEAD, mettant a jour dev-verifie.md et revue-increment/SKILL.md) ; seul le fichier agent-orchestrator/SKILL.md y a decroche (un unique commit, 0b414f9, sa creation). Le diagnostic du hub, lui, n'avait regarde que le mtime et le nombre de lignes, et en avait conclu a un « artefact empoisonne reste sur le disque ». Un fichier decroche dans un bundle vivant n'est pas un residu fige et n'appelle pas le meme geste. AUCUNE ACTION N'A ETE PRISE sur ce fichier : ni suppression, ni banniere, ni re-synchronisation. Le risque assume par cette annulation est nomme : les 120 lignes du 2026-07-21 restent techniquement deployables depuis ce bundle. Il est desormais tres reduit, parce que la source du deploiement a change — export/ du hub est genere depuis les sources vivantes et surveille par --check (48/48) et --check-flotte.
- **`veille:bmad-method-v7-annoncee-uv-standard-pas-sortie-derniere-rele`** (2026-09-01) : ECARTE : Ecartee : le titre dit lui-meme ANNONCEE, PAS sortie, et la verification a la source du 2026-07-30 l'a confirme (API GitHub releases/latest = v6.10.0 du 2026-07-03, aucun tag v7*, dist-tag npm latest = 6.10.0). Deux des trois arguments de l'entree etaient des faits de v6.9.0 DEJA installes. Surtout : la seule decision qu'elle bloquait — le gel « aucune customisation jusqu'a la v7 » — a ete LEVEE le 2026-07-31 sans attendre la v7, au motif qu'un gel conditionne a un evenement qui ne vient pas est un gel definitif qui ne dit pas son nom. Il ne reste donc rien a arbitrer ici. La migration redeviendra une decision a part entiere quand la v7 sortira vraiment ; la cadence de veille la re-signalera.
- **`veille:disler-claude-code-hooks-multi-agent-observability-observabi`** (2026-09-01) : ADOPTE + APPLIQUE (arbitrage utilisateur du 2026-09-01) — COMPLETE l'arbitrage veille:multi-agent-observability du 2026-07-31, qui avait INSTRUIT la trouvaille sans l'adopter et laissait DEUX pistes explicitement « en attente d'arbitrage ». Les voici tranchees. PISTE 1, APPLIQUEE : capturer la fin de sous-agent. log_usage.py capte SubagentStop — la FIN d'un sous-agent et non plus seulement son lancement : un sous-agent DISPATCHE et un sous-agent REVENU s'ecrivaient pareil, donc un fan-out dont une branche meurt etait indiscernable d'un fan-out complet. Plus le marquage d'un echec, mais UNIQUEMENT quand l'outil le dit : deviner « pas de succes donc echec » fabriquerait des KO qui n'ont pas eu lieu, et le superviseur compte les ko-repete. Hook cable dans settings.json ; forme des lignes existantes inchangee. PISTE 2, SANS OBJET SUR LE HUB, et c'est un constat pas un refus : un panneau « runs en cours » suppose des runs ouverts en resultat en-cours. Le hub n'en produit aucun (mesure du 2026-09-01 sur 91 runs : succes et partiel uniquement) — le journal en DEUX TEMPS est une specialisation LOCALE de VSCode, pas la regle du hub, dont la skill journalise a la fin du run. Construire le panneau ici afficherait un compteur toujours a zero. A rouvrir si le hub adopte un jour le journal en deux temps. ECARTE, inchange depuis le 2026-07-31 : la pile Bun + SQLite + Vue et son flux WebSocket — un process persistant contredit un hub qui regenere un wiki statique a 0 token ; depot dormant depuis le 2026-02-08 ; et la note de veille annoncait des « swim lanes par agent » qui n'existent pas. On adopte l'idee, jamais le code. TROUVE EN INSTRUISANT L'ADOPTION, et c'est le vrai gain : usage.jsonl portait 250 lignes et AUCUN LECTEUR depuis le 2026-07-23 — les compteurs de l'etage 1 viennent de state.json, pas d'ici. Elargir la captation sans donner un lecteur au journal aurait double la depense sans rien acheter : une adoption d'observabilite qui n'observe rien, exactement « l'outil qui tourne pour rien » que la salle revue-consommation existe pour nommer. lire_journal_usage() et render_journal_usage_html() le lisent et le rendent. Premiere lecture : 250 invocations sur 96 sessions du 2026-07-23 au 2026-09-01 — agent-orchestrator 88, general-purpose 74, Explore 32, agent-supervisor 15. VERIFIE : 12 tests dedies, 3 injections de faute (echec devine, rendu debranche, hook decable) vues ROUGES puis fichiers restaures a l'identique.
- **`.claude/hooks/warn_verif_before_commit.py`** (2026-09-01) : ACCEPTE + APPLIQUE (arbitrage utilisateur du 2026-09-01, « applique le premier ») — finding verification-manquante du diagnostic etage 2. DEFAUT : _verif_ran ne reconnaissait une verification que sous l'outil Bash, alors que PowerShell est le shell PRIMAIRE de cet environnement ; et sur le hub _VERIF_SKILL etait un tuple VIDE, rendant la branche Skill structurellement morte. Reproduit avant correction par appel direct sur transcripts synthetiques : Bash py -m pytest -> True, PowerShell py -m pytest -> False, Skill revue-increment -> False. Ce hook est le critere fort de la pastille Revue de code verte sur 6 depots sur 6 : elle reposait donc sur la PRESENCE du garde-fou et jamais sur son fonctionnement — le corollaire R6 exact. CORRECTIF : `if name in ("Bash", "PowerShell")` (les deux outils exposent la commande sous la meme cle input.command) et, au hub seul, _VERIF_SKILL = ("revue-increment",). PORTEE MESUREE, plus fine que le finding : le trou PowerShell etait UNIVERSEL (6 copies sur 6), mais la branche Skill morte etait propre au HUB — les 5 copies flotte portaient deja (pptx-verify, revue-increment). APPLIQUE sur le hub, VSCode, VSCode1, VSCode3, VSCode4 par EDITION CHIRURGICALE d'une ligne, jamais par recopie : les copies portent du local (VSCode1 : 295 l. contre 206 au hub) et la perte de contenu local du run 10:11 est trop fraiche. Kit republie depuis VSCode3, sa source au manifeste. VSCode2 NON CORRIGE ET ASSUME : le hub ne committe pas chez autrui sans mandat du proprietaire (R2/R4). Le correctif d'une ligne a ete propose a sa session ; l'exemption est ECRITE et datee dans tests/test_garde_fou_verif_shell.py, et un test echoue si un depot NON exempte redevient aveugle OU si l'exemption devient fausse. Les sessions de VSCode1/3/4 ont ete prevenues ; vscode4-5d a explicitement refuse de committer ma modification dans son lot (melanger un correctif de garde-fou a un lot deck ferait le commit non scope que R2 interdit) et de me donner un feu vert qu'elle n'a pas recu de l'utilisateur — les 4 editions restent donc NON COMMITEES chez les cibles, en attente d'arbitrage utilisateur. VERIFIE : 10 tests dedies dont 7 VUS ROUGES avant correction, 2 injections de faute (correctif annule, exemption mensongere) rouges puis fichiers restaures a l'identique. SIGNALE PAR vscode4-5d, non traite ici : _VERIF_BASH et _WATCHED_PREFIXES doivent coller au canal REEL de chaque projet. Mesure : ils SONT deja localises (npm/node chez VSCode et VSCode1, pytest+ruff chez VSCode2, pytest+test_generate_deck chez VSCode3/4). Le residu est etroit — une verif lancee en appelant LibreOffice ou PowerPoint COM directement au shell ne matcherait pas, la skill pptx-verify du tuple Skill la couvrant seulement si elle est invoquee comme skill. A instruire separement.
- **`.claude/dispositif/export_agentic.py --check-flotte`** (2026-09-01) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur du 2026-09-01, « traite tous les points identifiés dans la page pilotage »). DÉFAUT CONFIRMÉ ET AGGRAVÉ à la reprise : le détecteur annonçait 104 DIFFERENT (le finding en mesurait 97). LA CAUSE DU FINDING ÉTAIT INCOMPLÈTE — il n'avait vu que le bandeau « GÉNÉRÉ » ; mesuré, le retirer seul laisse la comparaison fausse sur les 5 dépôts, parce que sync_dispositif.write_crlf écrit les cibles en CRLF alors que le canon et export/ sont en LF. Classement exact des 104 avant correction : 12 bandeau+CRLF, 4 CRLF seul, 88 dérives réelles = 16 faux positifs (15,4 %). APPLIQUÉ : _lire_lf() + _sans_banniere() + _signature_propagation() dans export_agentic.py, réutilisant la MÊME découpe que sync_dispositif.strip_header (deux règles divergentes rouvriraient le trou) ; 4e statut SIGNATURE distinct de DIFFERENT, compté à part au total. RÉSULTAT MESURÉ : 104 -> 88 dérives réelles + 16 signatures ; le hub passe de 4 à 2 écarts sur son propre canon, et scan_transcripts.py / log_run.py — les deux SEULS fichiers du canon, donc les plus critiques — cessent d'être DIFFERENT en permanence sur les 6 dépôts. VERROUILLÉ par 5 tests dans tests/test_check_flotte.py, VUS ROUGES avant correction, dont test_une_vraie_derive_reste_signalee qui empêche la normalisation de devenir un blanchiment, et test_le_crlf_seul_suffit_a_fausser_le_verdict qui garde la cause que le finding avait manquée. 16/16 verts après.
- **`VScode5:CLAUDE.md`** (2026-09-01) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur du 2026-09-01, « traite tous les points identifiés dans la page pilotage »). Les quatre dérives re-mesurées à la reprise, toutes confirmées et toutes pires que la veille. APPLIQUÉ (a) : deux blocs à marqueurs CHIFFRES-MESURES:VOLUMINEUX et :REPRISES dans CLAUDE.md, régénérés par scan_projets.py (bloc_volumineux() par os.path.getsize, trié du plus gros au plus petit ; bloc_reprises() lu dans routing-hints.json), sur le patron des marqueurs TODO-AGENTS déjà en place. Écriture en newline='' : CLAUDE.md est en LF et le mode texte Windows l'aurait repassé en CRLF, soit 132 lignes de diff à chaque scan pour deux chiffres — le churn que R2 interdit. (b) : les trois « 9 salles » en dur remplacés par nb_salles(), qui compte les en-têtes workflow.party_groups du TOML source — le kit publié annonçait 9 pour 12 réelles ; kit régénéré, 48/48, 0 dérive. (c) : la ligne « dette assumée » de tests corrigée — mesuré, scan_projets.py est exercé par 26 fichiers de tests/, scan_transcripts.py par 8, log_run.py par 7 sur 38 : la dette était éteinte depuis longtemps et la règle faisait renoncer à ce qui existait. (d) : idempotence verrouillée par tests/test_chiffres_mesures.py (10 tests) — deuxième passe sans changement, fins de ligne préservées, tailles égales au disque, tri décroissant, README publié aligné sur le TOML. EFFET DE BORD ASSUMÉ : l'argument de R6 opposant un playbook sûr à un playbook risqué a été RÉÉCRIT, parce que le bloc régénéré le contredit — evolution-flotte 0,60 reprise par run contre dev-verifie 0,62. C'est le bloc généré qui a invalidé la prose qu'il illustrait.
- **`salles:accueil-projet,conseil-flotte,atelier-deck,mise-en-service`** (2026-09-01) : ACCEPTÉ PARTIELLEMENT + APPLIQUÉ (arbitrage utilisateur du 2026-09-01 : « rien retirer, poser la règle anti-récidive »). LA PROPOSITION PRINCIPALE ÉTAIT DÉJÀ SATISFAITE, vérifié avant d'écrire (R1) : les quatre salles ont chacune un déclencheur nommé dans le bloc SALLES-ROUTAGE de la skill agent-orchestrator, et le test que le finding demandait d'ajouter — « chaque salle du TOML actif doit avoir au moins un déclencheur » — existe déjà dans tests/test_salles_routage.py. La branche (a) du finding n'avait donc rien à appliquer, et lui ajouter une ligne de routage n'aurait rien changé. MESURE REFAITE, plus large que celle du finding, sur les 97 runs : les salles à ZÉRO convocation ne sont pas les quatre nommées mais accueil-projet, code-review-crew, inspection-critique et anti-consensus-club ; conseil-flotte, atelier-deck, mise-en-service et socle-technique en ont une — leur run de création ; atelier-idees 7, atelier-dev 4, revue-consommation 3, observatoire-agentic 2. APPLIQUÉ : la règle anti-récidive est inscrite au § 2 septies de la skill — une salle neuve n'entre dans le kit publié qu'après une convocation réelle sur une demande utilisateur, jamais sur son test de câblage — avec les chiffres mesurés et la mise en garde contre la lecture inverse (le déclencheur n'est pas ce qui manque). AUCUNE MISE EN SOMMEIL : trois des quatre salles à zéro dataient de la veille, les juger à un jour aurait été ne pas leur laisser leur chance. CONSÉQUENCE ASSUMÉE ET SIGNALÉE : éditer la skill met le socle des 5 cibles en retard, donc 2 tests de tests/test_check_flotte.py au rouge ; ils ne se referment que par une propagation, laquelle est le chantier explicitement PARQUÉ par l'utilisateur en début de séance. L'audit technique du même jour qualifie indépendamment ces 2 tests de défaut (ils assèrent une propriété du monde — l'état de 5 dépôts tiers — et non du code).
- **`VScode5:audit-technique-perime`** (2026-09-01) : ACCEPTÉ + APPLIQUÉ dans ses DEUX gestes (arbitrage utilisateur du 2026-09-01, « traite tous les points identifiés dans la page pilotage »). (a) audit-technique RELANCÉ sur le hub, cadré sur la surface réellement nouvelle (.claude/dispositif/ export_agentic, propager_socle, sync_dispositif, package/install_agentic ; ecarter_trouvaille ; point_du_jour ; log_usage) et non sur le dépôt entier. Verdict écrit dans .claude/audits/VScode5.json le 2026-09-01 : robustesse CRITIQUE, risque_technique CRITIQUE, securite CRITIQUE, performance ok — contre un risque_technique « ok » qui valait pour un périmètre inexistant à la date du précédent audit. 14 défauts, dont le plus grave confirme la crainte qui fondait le finding : propager_socle.lignes_perdues compare le chapitre local À LUI-MÊME (composer() le recolle verbatim), donc le garde-fou anti-perte NE PEUT PAS se déclencher — reproduit sur les 5 copies réelles, perdues=0 partout. C'est la TROISIÈME occurrence du même défaut en un jour : un garde-fou qui compare autre chose que ce qu'il prétend comparer. Deux autres critiques touchent l'installateur auto-portant : traversée de chemin non validée (destination ../DEPOT_VOISIN acceptée, écriture hors cible, exit 0) et --force qui écrase le CLAUDE.md rédigé de la cible par un squelette vide, sans sauvegarde. (b) PÉREMPTION OUTILLÉE dans scan_projets.py : audit_perime() pose la DOUBLE condition demandée — plus de CADENCE_AUDIT_J jours ET plus de AUDIT_LIGNES_SEUIL lignes changées depuis la date de l'audit, mesurées par git log --numstat (0 token). Le retard remonte au poste de pilotage avec SES DEUX CHIFFRES, parce que « périmé depuis 34 j » seul ne dit pas s'il y a de quoi le repayer. Il discrimine réellement : VSCode (33 j mais 669 lignes) n'est pas signalé, les 5 autres l'étaient de 5 750 à 42 647 lignes. Verrouillé par tests/test_audit_peremption.py (9 tests), dont la PAIRE qui prouve la discrimination sur deux dépôts git témoins fabriqués — l'un gelé, l'autre très modifié, même âge d'audit. Les 14 défauts de l'audit sont désormais des constats ouverts à arbitrer, non traités par ce run.
- **`VScode5:audit-technique-2026-09-01`** (2026-09-01) : ACCEPTÉ + APPLIQUÉ — LES 14 DÉFAUTS DE L'AUDIT (arbitrage utilisateur du 2026-09-01, « traite les 34 constats »). Chaque correctif est verrouillé par des tests VUS ROUGES avant correction ; suite finale 713 passés / 0 échec, couverture 75 %, kit 48/48 sans dérive. CRITIQUES (7) : (1) propager_socle.lignes_perdues comparait le chapitre local À LUI-MÊME — composer() le recolle verbatim, donc la soustraction était vide par construction et la branche fichier-entier inatteignable ; le garde-fou anti-perte protégeait la seule chose qui ne risquait rien. Il compare désormais AUSSI le hors-chapitre, la seule zone que composer() reconstruit depuis le socle, donc la seule où une ligne locale se perd ; mesuré avant d'écrire : 0 ligne signalée sur les 5 dépôts, la correction ne re-bloque pas la propagation qu'elle protège. (2) install_agentic acceptait une destination « ../DEPOT_VOISIN » ou absolue du MANIFESTE.json — qui voyage avec le kit — et écrivait hors cible en rapportant « ecrit », exit 0 ; _sous_la_cible() refuse désormais, avec le cas nominal préservé par test. (3) --force écrasait le CLAUDE.md rédigé de la cible par le squelette vide sans sauvegarde ; CLAUDE.md n'appartient pas au kit et n'est plus jamais écrasé, le squelette est posé en CLAUDE.md.propose. (4) log_usage avalait toute panne d'écriture en exit 0 avec stderr VIDE, contre son propre docstring (« ne perd plus rien en silence ») ; les deux moitiés de la promesse sont désormais tenues et testées ensemble. (5) test_propager_socle acceptait les TROIS états atteignables — assertion vacante, réécrite en assertion unique. (6) les 2 tests rouges de test_check_flotte assertaient l'état de 5 dépôts tiers : refermés par la propagation du socle, autorisée par l'utilisateur après le lot A. (7) sync_dispositif distinguait « dérive (en-tête) » de « DÉRIVE (corps) » pour les AFFICHER puis écrasait les deux pareil ; la seule écriture destructrice est refusée, franchissable par --accepter-derive comme le --accepter-pertes de propager_socle, et le mode par défaut est conservé (le bandeau propagé dans les 6 copies documente la commande sans option). MOYENS ET FAIBLES (7) : (8) _fusionner_settings plantait en AttributeError sur un settings.json de forme inattendue, APRÈS la copie des 47 fichiers, sans rollback ni checklist — il refuse maintenant sans jamais écraser les clés du projet cible ; (9) le socle était lu dans export/ sans contrôle de fraîcheur, alors qu'à 15:20 --check rendait exit 1 pendant que la propagation annonçait 5 × a-propager en exit 0 : _socle_perime() bloque désormais la propagation d'un artefact périmé ; (10) point_du_jour fermait par contenance de sous-chaîne — un arbitrage humain en enterrait deux (mesuré : veille:awesome-claude-code fermait aussi awesome-claude-code-subagents) ; la désambiguïsation est faite dans trouvailles_ouvertes(), seul endroit qui voit toutes les entrées, et attribue l'arbitrage à la correspondance la plus proche ; (11) codes de sortie : propager_socle rendait 0 en refusant toutes les cibles, verifier_flotte rendait 0 même sans rien avoir comparé ; (12) --check prescrivait « régénérer » sur un ORPHELIN que generer() ne supprimait jamais — un remède qui ne remédie pas finit ignoré ; (13) _projets_flotte n'excluait pas le hub, qui se comparait donc à son propre export/, dont 2 écarts structurellement insolubles ; (14) l'ancre « ## Méthode — 5 étapes » vivait en littéral dans 3 fichiers sans rien qui les lie — un test verrouille leur égalité. RESTE À FAIRE, SIGNALÉ : les 4 dimensions de .claude/audits/VScode5.json (robustesse, risque_technique, securite CRITIQUES) ont été cotées AVANT ces correctifs — elles doivent être re-cotées par un nouvel audit, que la péremption au calendrier ne signalera pas puisque le fichier date du jour.
- **`.claude/skills/pptx-framed-image/scripts/stock_images.py`** (2026-09-01) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur du 2026-09-01, « traite les 34 constats »). FAILLE TROUVÉE PAR UNE CIBLE, PAS PAR LE HUB : la session VSCode3, en instruisant son finding ppt-toolkit.md, a découvert que sa copie locale portait une garde que les 6 autres copies de la flotte n'avaient pas — dont CELLE-CI, qui est la source du kit. Sa propre doctrine de resynchronisation, écrite noir sur blanc (« VSCode1 source de vérité, à re-synchroniser manuellement »), aurait donc SUPPRIMÉ un correctif de sécurité en la réalignant sur nous. DÉFAUT : fetch_to() passait à urllib.request.urlopen une URL venant d'Openverse — un agrégateur de sources tierces (Wikimedia, Flickr, StockSnap), donc une donnée non contrôlée — sans vérifier son schéma. Or urlopen suit file:// par défaut : REPRODUIT en une ligne, il lit un fichier local. Une entrée dont l'url n'est pas http(s) faisait recopier un fichier arbitraire du poste dans le cache d'images du deck. Et r.read() sans plafond chargeait toute la réponse en mémoire : un serveur tiers décidait de la mémoire de la machine. APPLIQUÉ : _SCHEMES_AUTORISES (http/https) et _TAILLE_MAX (25 Mo) avec lecture par blocs de 64 Ko, patch repris tel quel de VSCode3 plutôt que réécrit. VERROUILLÉ par 3 tests dans .claude/skills/pptx-framed-image/tests/, dont celui de la SSRF vu ROUGE (« CONTENU_LOCAL » retrouvé dans le cache d'images) et celui du plafond réécrit en pytest.raises après avoir constaté qu'il passait par construction. Kit régénéré : la garde est dans export/, elle partira à la prochaine installation. RESTE OUVERT : VSCode1, source amont de ce script, reste vulnérable (70 l., 0 occurrence de la garde) — traité par sa propre session ou à propager.
- **`VScode5:re-cotation-audit-2026-09-01`** (2026-09-01) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur du 2026-09-01, « traite le point 3 »). RE-COTATION FAITE, sur le code SUR DISQUE et non sur celui d'il y a quelques heures : robustesse CRITIQUE → MOYEN, risque_technique CRITIQUE → MOYEN, securite CRITIQUE → MOYEN, performance ok. Les 14 défauts sont fermés ou partiellement fermés, chacun re-vérifié par exécution, et la re-cotation a fait ce pour quoi elle était mandatée : CHERCHER CE QUE MES CORRECTIFS AVAIENT CASSÉ. Elle a trouvé trois régressions de mon fait, déjà poussées, toutes trois corrigées dans la foulée. (1) LA PLUS GRAVE — en ajoutant la comparaison hors-chapitre à `lignes_perdues`, j'avais recréé le défaut SYMÉTRIQUE de celui que je corrigeais : la copie cible vaut `ancien_socle + provenance + chapitre`, donc « présent avant, absent après » contenait toute phrase que le hub avait REFORMULÉE entre-temps. Reproduit : réécrire UNE ligne du socle faisait passer les 5 cibles en PERTE-LOCALE, la ligne « perdue » étant une phrase du hub. Le garde-fou bloquait la propagation qu'il protège. Un garde-fou muet et un garde-fou qui crie à tort sont le même défaut vu des deux côtés — ce fichier l'a appris deux fois dans la même journée. CORRIGÉ par `socle_d_origine()` : la copie porte le hash de sa génération dans sa propre ligne de provenance, `git show <hash>:export/...` rend l'ANCIEN socle, et ce qu'il contenait est attribuable au hub, jamais au local. Vérifié sur le chemin réel via `traiter()` : reformulation du hub → `a-propager`, 0 perdue ; ligne locale hors chapitre → toujours signalée. (2) La garde SSRF arrêtait le téléchargement au plafond mais LAISSAIT SUR DISQUE les 25 Mo déjà écrits, sous le nom de l'image attendue (mesuré : 26 214 400 octets) — un refus qui laisse son échec derrière lui n'est un refus qu'à moitié. Corrigé, `BaseException` pour couvrir aussi l'interruption clavier. (3) `--accepter-pertes`, qui parle des lignes LOCALES, court-circuitait aussi le contrôle de FRAÎCHEUR du socle : forcer une perte assumée désactivait sans le dire un contrôle qu'on n'avait pas voulu lever. La fraîcheur est désormais bloquante sans exception — la lever coûte 5 dépôts faux, la réparer coûte une commande. TROIS TESTS AJOUTÉS PAR RÉGRESSION, tous vus ROUGES avant correction, plus un quatrième qui ferme le trou de mutation M2 signalé par la re-cotation (retirer `- util(socle)` ne faisait échouer aucun test, le seul cas couvert ne faisant que GROSSIR le socle). Suite : 720 passés / 0 échec, couverture 75 %, kit 48/48. LA RE-COTATION A AUSSI VALIDÉ LES GARDE-FOUS : en rejouant les tests neufs contre les sources d'AVANT, 20 passent au rouge — ils gardent bien ce qu'ils annoncent. RESTE OUVERT, remis à l'utilisateur : le défaut n°6 est refermé par un ÉTAT DU MONDE et non par une refonte (14 des 21 tests de test_check_flotte tournent contre les 5 dépôts réels) ; `_fusionner_settings` rend ECHEC mais exit 0 avec la checklist de succès ; le refus de `sync_dispositif` sort 0 en écriture alors que le même état sort 1 en `--check` ; une cible absente et même « tous dépôts introuvables » sortent 0 ; `ftp://` reste suivi en redirection et `search_photo` n'a pas de plafond.
- **`veille:microsoft-hve-core-skill-powerpoint-python-pptx-pilot-e-par`** (2026-09-01) : ECARTE : Ecartee sur arbitrage utilisateur du 2026-09-01, apres 40 jours en statut etudie. La decision etait DEJA prise et tracee le 2026-07-31 (« INSTRUIT, MIGRATION ECARTEE ») : la skill PPT de hve-core vit sous .github/skills/EXPERIMENTAL/, non stabilisee par Microsoft lui-meme, et migrer VSCode3/VSCode4 vers un pipeline YAML serait une refonte sans point de douleur identifie — contre R1, correction minimale > refonte. Seul le STATUT etait reste 'etudie', ce qui faisait annoncer chaque matin comme en attente une trouvaille deja tranchee. C'est exactement la panne que le finding veille:trouvailles-dormantes denonce : une decision prise mais non reinjectee dans l'etat.
- **`veille:dev-browser-l-agent-v-rifie-son-travail-dans-un-vrai-navigat`** (2026-09-01) : ECARTE : Ecartee sur arbitrage utilisateur du 2026-09-01, apres 34 jours en statut etudie — la plus ancienne attente du dispositif, annoncee chaque matin par le hook. L'instruction du 2026-07-31 avait deja fait le travail : le sandbox QuickJS WASM restreint le SCRIPT ecrit par l'agent, pas le navigateur, et la trouvaille est SANS OBJET pour VSCode1, qui pilote deja un vrai Chrome sur localhost:3000 via app/scripts/capture-screenshots.js. L'objet reel se limitait a VSCode2 (FastAPI+HTMX), sans point de douleur mesure a ce jour. Ecarter n'interdit rien : si la verification en navigateur devient un besoin reel sur VSCode2, elle se rouvre par un arbitrage neuf, sur un besoin constate plutot que sur une possibilite.
- **`VScode5:re-cotation-audit-2026-09-01`** (2026-09-01) : RECTIFICATION D'UN CHIFFRE (2026-09-01, passe de vérification des 110 arbitrages) — l'entrée précédente sur cette cible annonçait « Suite : 720 passés / 0 échec » sans dire SUR QUOI. Mesuré : `py -m pytest tests/ -q --collect-only` rend **705** — c'est le périmètre que la table de vérification du CLAUDE.md documente — et `py -m pytest tests/ .claude/skills/pptx-framed-image/tests/` rend **718**, le périmètre que j'avais réellement lancé. Aucun des deux ne fait 720. Le nombre était exact à l'instant de la mesure et ne l'est plus : la mise en sommeil de 4 porteurs a retiré 4 cas au test paramétré `test_les_porteurs_ont_bien_l_outil_skill`, et deux tests ont été ajoutés depuis. LA MOITIÉ UTILE DE L'AFFIRMATION TIENT — la suite est verte, 0 échec — mais le compte n'était pas reproductible par la commande documentée, ce qui est très exactement la faute que R6 nomme, commise dans un arbitrage dont l'objet était de vérifier des chiffres. LEÇON, et elle vaut au-delà de ce cas : un compte de tests n'est PAS un fait stable, c'est une mesure horodatée qui dépend d'un périmètre. L'écrire sans sa commande ET son périmètre le rend faux dès le lendemain — les cinq partiels de la tranche 1 sont tous de cette famille (comptes de tests, de commits, de lignes, d'assertions), et ils datent tous d'AVANT l'adoption de R6. Trouvé par la tranche 4 de la passe de vérification, pas par moi.
- **`flotte:rtk-settings-local`** (2026-09-01) : OUVERT PAR LA PASSE DE VÉRIFICATION DU 2026-09-01, non traité — remis à l'utilisateur. L'arbitrage `flotte:rtk` du 2026-07-29 annonce le retrait de rtk « de toute la flotte », permissions comprises. Vérifié : le canal VERSIONNÉ est bien purgé (aucune occurrence dans les `.claude/settings.json`, ni dans le settings global), et la skill `rtk-usage` a disparu des dépôts. MAIS le canal LOCAL ne l'est pas : `Bash(rtk *)` survit dans TROIS `settings.local.json` — VSCode (`rtk curl`), VSCode1 (8 entrées : pip, ps, find, ls, diff, wc, du, npm) et VSCode2 (find, read), soit 11 permissions actives sur 3 dépôts sur 5. L'ensemble de permissions EFFECTIF autorise donc toujours rtk là où le titre dit qu'il est retiré. LA CAUSE EST STRUCTURELLE, pas un oubli : `settings.local.json` est git-ignoré, donc un commit scopé (R2) ne pouvait par construction pas le voir — le retrait s'est arrêté à la frontière du versionné sans que rien ne le signale. DEUX SUITES POSSIBLES, à arbitrer : (a) retirer les 11 entrées des trois fichiers locaux — aucun commit possible, la trace ne peut être qu'un arbitrage ; (b) rectifier le périmètre déclaré de `flotte:rtk` en « permissions versionnées ôtées, canal local hors périmètre ». Dans les deux cas, un troisième geste se recommande de lui-même : faire entrer le balayage des `settings.local.json` dans le scan, puisqu'un retrait de flotte s'y arrête aujourd'hui EN SILENCE.
- **`flotte:rtk-settings-local`** (2026-09-01) : ACCEPTE + APPLIQUE (option « purger + outiller », arbitree par l'utilisateur le 2026-09-01). DEUX GESTES, pas un. (1) PURGE : les 11 permissions `Bash(rtk ...)` sont retirees des trois `settings.local.json` — VSCode 1 (curl), VSCode1 8 (pip, ps, find, ls, diff, wc, du, npm), VSCode2 2 (find, read) ; chaque fichier relu apres ecriture, JSON valide. Aucun commit possible et aucun attendu : ces fichiers sont git-ignores, la trace ne peut etre que cet arbitrage. (2) OUTILLAGE, qui est la vraie reponse : `scripts/scan_projets.py` porte desormais `permissions_par_canal()`, qui lit les DEUX canaux et expose `local_seules` — les permissions que seul le fichier git-ignore porte, donc precisement celles qu'un retrait par commit ne peut pas atteindre. Les trois familles (allow/ask/deny) sont balayees, et l'union sert desormais aux proxies securite : un `deny` ou un hook `guard_destructive_git` pose en local etait jusqu'ici compte pour rien — le canal local n'etait pas seulement un silence, c'etait une SOUS-EVALUATION. Verrouille par `tests/test_scan_permissions_locales.py` (5 tests, les 5 vus ROUGES avant correctif, dont celui qui rendait « aucun garde-fou » sur un projet portant un deny). MESURE APRES COUP, et elle depasse rtk : 105 permissions locales-seules sur la flotte (VSCode1 89, VSCode2 16). RESTE, SIGNALE ET NON TRAITE — hors du perimetre des 11 nommees par l'arbitrage : deux permissions de VSCode1 sont des commandes composees qui APPELLENT rtk sans etre des `Bash(rtk ...)` (un `cd ... && rtk ls`, un `cp ... && rtk diff`). Elles sont mortes puisque l'outil l'est, mais elles sont toujours la, et je ne les ai pas retirees de ma propre initiative.
- **`VScode5:propager_socle-provenance-arbre-sale`** (2026-09-01) : ACCEPTE + APPLIQUE (option « refuser de propager sur socle sale », arbitree par l'utilisateur le 2026-09-01). DEFAUT TROUVE EN CHERCHANT POURQUOI LA PROPAGATION REFUSAIT, et prouve avant d'etre corrige : `hash_hub()` estampille HEAD, alors que le socle reellement copie est lu dans l'ARBRE DE TRAVAIL. Les 5 cibles portaient `SOCLE-PROVENANCE: 604fc7c` en contenant un paragraphe entre dans l'histoire en `0f4e632` seulement — `git show 604fc7c:<socle> | grep -c` rend 0, `0f4e632` rend 1, la copie rend 1. Consequence a la propagation SUIVANTE : `socle_d_origine()` remonte un socle ampute et `lignes_perdues()` classe en PERTE-LOCALE chaque phrase ajoutee par le hub depuis HEAD — 10 lignes sur 5 cibles, propagation bloquee, 3 tests rouges. C'est la QUATRIEME occurrence, dans ce meme fichier, du garde-fou qui compare autre chose que ce qu'il pretend comparer ; le correctif `socle_d_origine()` du 2026-09-01 avait ferme le cas symetrique en faisant confiance a la provenance, sans verrouiller la condition qui fonde cette confiance. CORRECTIF : `_socle_non_commite()` refuse la propagation quand le socle publie porte des modifications non commitees, BLOQUANT et sans exception — `--accepter-pertes` arbitre des lignes locales, il n'arbitre pas une provenance fausse inscrite chez cinq tiers (c'est exactement la regression du meme jour sur `_socle_perime`, qu'on ne recommet pas). La porte tombe AVANT `ligne_provenance`, verrouille par un test qui fait exploser la fabrication de la provenance. Fail-open sur un git muet. 6 tests, les 6 vus ROUGES avant correctif. CORRIGE AU PASSAGE : le message de refus melangeait `sans-chapitre-local` et `PERTE-LOCALE` et envoyait le lecteur creer un chapitre qui existe deja — il dit maintenant, pour chaque cause, le geste qui la leve.
- **`flotte:rtk-settings-local`** (2026-09-01) : RECTIFICATION D'UNE AFFIRMATION NON MESUREE, par le diagnostic etage 2 du meme soir. L'entree precedente sur cette cible ecrit que le canal local n'etait pas qu'un silence mais une SOUS-EVALUATION, « un deny ou un hook guard_destructive_git pose en local etait compte pour rien ». C'est vrai du CODE d'avant le correctif et FAUX de la flotte : mesure sur les 5 depots, deny versionne True partout, deny local False partout, hook local False partout. Le superviseur l'a etabli independamment par un A/B sur les 6 projets en neutralisant settings.local.json — zero ecart de niveau sur les 9 dimensions, seul le libelle change. La sous-evaluation vaut donc 0 aujourd'hui. CE QUI RESTE VRAI : la mesure du canal local (105 permissions locales-seules) et le fait qu'un retrait de flotte par commit ne peut pas les atteindre — c'etait le motif de l'arbitrage et il tient. CE QUI ETAIT DE TROP : avoir presente un risque POSSIBLE comme un defaut CONSTATE, dans un arbitrage dont l'objet etait precisement de fermer un angle mort de mesure. R6 s'applique a celui qui l'invoque. Le message du commit f7f43cd porte la meme phrase et ne peut pas etre reecrit ; cette entree est sa rectification.
- **`VSCode1:.claude/settings.local.json`** (2026-09-01) : ACCEPTE + APPLIQUE. Defaut de MA main, introduit le matin meme par le correctif qui devait fermer l angle mort du canal local : `projets-supervision.md` rendait « 🟢 deny rules, guard git, 89 perm. hors git » sous la legende « Garde-fous PRESENTS ». Les 89 permissions git-ignorees de VSCode1, jamais relues — dont `Read(//c/Users/claude.camus/**)`, deux `Edit` sur les skills globales de l utilisateur, `Bash(node -e ...)` a joker et `Skill(run:*)` — se lisaient comme un TROISIEME garde-fou, meme liste, meme pastille. Une EXPOSITION rendue comme une PROTECTION : la famille de defaut que la journee entiere corrigeait, commise dans la mesure censee la fermer. DEUX GESTES : (1) `local_seules` sort de la liste des garde-fous et s affiche a part, marque — « 🟠 deny rules, guard git · ⚠ 89 perm. hors git, jamais relues par un commit » ; (2) il entre dans la NOTATION, en PLAFOND et non en penalite : un ensemble de permissions qu aucun commit ne peut relire n est pas une posture verte, mais un projet sans permission hors git atteint le vert comme avant. CONSEQUENCE ASSUMEE ET MESUREE : VSCode1 et VSCode2 passent de 🟢 a 🟠 sur la dimension securite, et VSCode2 bascule en alerte projet `critique`. C est le but — la note dit desormais ce que le detail signalait. 6 tests, les 4 qui pouvaient echouer vus ROUGES avant correctif.
- **`.claude/dispositif/propager_socle.py::traiter`** (2026-09-01) : ACCEPTE + APPLIQUE. Trouve par `bmad-review-edge-case-hunter`, dispatche par le superviseur en contexte frais — et il a PROUVE un defaut plus grave que celui de son donneur d ordre, contredisant sa proposition initiale. Le seul chemin de ce depot qui ECRIVE chez autrui etait le moins protege des trois. (1) `--accepter-pertes` DETRUISAIT SANS RIEN IMPRIMER : la cle `perdues` n existait que dans le retour de REFUS, jamais dans le retour applique, donc `main()` faisait `r.get('perdues', [])` et affichait une liste vide pendant que des lignes disparaissaient — et le detail annoncait « chapitre local preserve » juste apres l amputation. Un drapeau qui existe pour ASSUMER une perte doit d abord la MONTRER. (2) Aucune verification de l etat de la cible : ecraser la copie d un depot ou une autre session a du travail non commite est exactement la faute que R2 nomme, et rien ne l empechait — nouvel etat bloquant CIBLE-SALE, avec lecture EXPLICITE du returncode. (3) Une seule cible illisible (cp1252) faisait remonter l exception hors de `main()` APRES avoir deja reecrit les depots precedents et sans jamais atteindre les suivants : une propagation a moitie faite dont personne n avait la liste — chaque cible est desormais enveloppee, etat ILLISIBLE, bloquant. REPRISE SIGNALEE : ma premiere version des deux premiers tests monkeypatchait `extraire_chapitre_local` pour fabriquer la perte ; elle ne pouvait pas marcher, `lignes_perdues` appelant elle-meme cette fonction sur l avant ET sur l apres, donc le patch rendait la difference vide. Le test mesurait le patch, pas le code. Refait sur le scenario reel — une ligne locale tissee HORS du chapitre, le cas exact du `--retrait-citation-mm 3.53` de VSCode2. [CIBLE RECTIFIEE le 2026-09-01 : elle etait ecrite tronquee, sans le prefixe de chemin du finding. La regle du hub dit « arbitrage trace a la cible EXACTE » et elle est mecanique, pas cosmetique : le scan referme un finding en comparant les chaines, donc trois constats traites seraient restes affiches en attente au wiki.]
- **`.claude/dispositif/propager_socle.py::_socle_non_commite`** (2026-09-01) : ACCEPTE + APPLIQUE. CINQUIEME occurrence de la famille dans ce fichier, et elle est dans MA porte du matin. Elle lisait `git status --porcelain` et seulement son `stdout` — trois trous, tous « la sonde ne mesure pas ce qu elle dit » : (1) hors depot git, rc=128 et stdout vide, elle repondait « propre » alors que `hash_hub()` rend « inconnu », qui n est pas une revision ; (2) sous `git update-index --assume-unchanged`, `status` reste vide alors que le fichier differe du blob — elle laissait passer le cas meme qu elle vise ; (3) sous `core.autocrlf=true`, la config REELLE de ce depot (verifie : `git config core.autocrlf` rend `true`), un fichier de travail en CRLF pouvait la faire crier alors que son texte EST le blob, et le remede qu elle imprime sort « nothing to commit » — un refus NON LEVABLE, pire qu un trou. CORRECTIF : on ne raffine pas la lecture de `status`, on CHANGE D ORACLE — comparer le CONTENU au blob HEAD, la seule question qui compte, insensible aux trois cas. `returncode` lu explicitement, fins de ligne normalisees des deux cotes, `hash_hub()` valide contre une expression hexadecimale. CHANGEMENT DE COMPORTEMENT ASSUME : git injoignable BLOQUE desormais, la ou la v1 faisait fail-open « comme _socle_perime ». Les deux ne repondent pas a la meme question — `_socle_perime` compare deux fichiers du disque et peut s abstenir sans consequence, celle-ci decide s il est licite d ECRIRE une affirmation chez cinq tiers pour toute la duree de vie des copies. AUSSI : `socle_d_origine` irresolvable degradait en silence contre un docstring promettant qu il « le dit » — `traiter` rend maintenant une cle `provenance` resolue/irresolvable. 5 tests neufs vus ROUGES, plus 2 de mes tests du matin RECRITS parce qu ils simulaient l ancien oracle et ne mesuraient donc plus le code. [CIBLE RECTIFIEE le 2026-09-01 : elle etait ecrite tronquee, sans le prefixe de chemin du finding. La regle du hub dit « arbitrage trace a la cible EXACTE » et elle est mecanique, pas cosmetique : le scan referme un finding en comparant les chaines, donc trois constats traites seraient restes affiches en attente au wiki.]
- **`.claude/supervision/scan_transcripts.py:807`** (2026-09-01) : ACCEPTE + APPLIQUE. Le meme script calculait « en sommeil » a deux endroits sur deux ensembles differents : `{**skills, **subagents}` pour `routing-hints.json`, `skills` seul pour le TODO de `docs/wiki/index.md`. Mesure du desaccord : 6 noms d un cote, 7 de l autre. LE MECANISME, verifie et plus precis que « deux definitions » : `{**skills, **subagents}` ECRASE l entree skill par celle du sous-agent de meme nom, et deux noms vivent dans les deux canaux — `agent-supervisor` (skill.last 2026-07-30, subagent.last 2026-09-01T19:17) et `veille-agentic` (2026-07-29 / 2026-08-31). Le TODO proposait donc d eteindre `agent-supervisor` LE JOUR OU il avait tourne, et `veille-agentic` le lendemain de sa derniere veille, tout en OMETTANT `bmad-recherche`, sous-agent pur, le seul reellement dormant. Un TODO qui propose d eteindre ce qui vient de servir et tait ce qui dort n est pas imprecis, il est A L ENVERS. CORRECTIF : fonction unique `dormants(state)`, qui prend le MAXIMUM des deux dates plutot que l ecrasement de dict — celui-ci tombait juste ICI par hasard, le sous-agent etant le plus recent, et aurait manque le cas symetrique. Verifie apres coup : les deux consommateurs rendent la MEME liste de 6, `agent-supervisor` et `veille-agentic` en sortent, `bmad-recherche` y entre. REPRISE SIGNALEE : ma premiere passe a introduit un `NameError` (build_todos ne recoit pas `state`), et le hook du scan l a AVALE EN SILENCE — « scan ignore (NameError) ». Corrige en passant `state` explicitement ; le fait que le scan absorbe une exception sans la faire remonter reste ouvert et non traite. [CIBLE RECTIFIEE le 2026-09-01 : elle etait ecrite tronquee, sans le prefixe de chemin du finding. La regle du hub dit « arbitrage trace a la cible EXACTE » et elle est mecanique, pas cosmetique : le scan referme un finding en comparant les chaines, donc trois constats traites seraient restes affiches en attente au wiki.]
- **`flotte:agent-orchestrator-SKILL.md`** (2026-09-01) : ACCEPTE + APPLIQUE (option « corriger l outil d abord, propager ensuite », arbitree par l utilisateur). Les 5 depots servaient une table markdown CASSEE — le paragraphe des porteurs endormis insere AU MILIEU du tableau des sous-agents, deux rangees orphelines hors table — publiee par 0f4e632 et reparee au hub seulement par f7f43cd. LA PROPAGATION A EXIGE TROIS GESTES, dans cet ordre : (1) corriger un faux positif de MA porte de provenance, qui refusait TOUTE propagation parce que `subprocess.run(text=True)` decode avec l encodage LOCAL (cp1252 ici) et non en utf-8 — le blob revenait mutile des sa 87e position, 400 lignes accentuees dans le socle, 0 survivante ; (2) rendre VRAIE la provenance deja inscrite chez les cibles, qui designait 604fc7c alors que leur socle n entre dans l histoire qu en 0f4e632, puis la COMMITTER chez elles (4 commits scopes au seul SKILL.md ; VSCode2 avait deja ete committe par sa propre session, f2a0520) ; (3) propager. VERIFIE AVANT D ECRIRE : sur les 50 lignes que le garde-fou signalait a travers les 5 copies, 0 est du travail local — toutes appartiennent a un socle du hub. VERIFIE APRES : 0 rangee orpheline sur les 5, chapitres locaux preserves (36, 45, 55, 52 et 32 lignes). Le sync du canon a suivi, avec rejeu des suites cibles impose par l outil : VSCode2 49 passes, VSCode3 30 passes. R2 tenu : les 1 a 13 fichiers non commites d autres sessions sont intacts.
- **`VScode5:scan-exception-silencieuse`** (2026-09-01) : ACCEPTE + APPLIQUE (demande utilisateur « traite le pb du scan avale une exception en silence »). Ne du vecu : un NameError introduit par moi dans `build_todos` a ete absorbe par le `except Exception` du point d entree, seule trace « Supervision agents : scan ignore (NameError...) », code de sortie 0 — la regression est restee invisible pendant deux commandes. CE QU ON NE TOUCHE PAS : le rattrapage lui-meme, ni le code 0 par defaut ; un hook SessionStart qui leve bloque l ouverture de session, ce qui est pire. CE QU ON CORRIGE, trois manques : le mot « ignore » ment sur la nature de l evenement (on ecrit ECHEC) ; aucune localisation (on sort la pile complete) ; aucune trace durable (journal `scan_incidents.jsonl`, et le scan SUIVANT remonte l incident puis l acquitte — c est le point qui repare la visibilite de la DUREE et pas seulement de l instant). `AGENT_SUPERVISION_SCAN_STRICT=1` rend le plantage fatal pour les tests et la CI. Corrige AU CANON puis resynchronise aux 6 depots. 7 tests vus ROUGES, plus une verification sur le chemin reel : panne fabriquee, ECHEC + pile + incident enregistre + code 0, puis code 1 en strict, puis remontee au scan suivant. DEUX MESSAGES DE MA MAIN RESSERRES EN COURS DE ROUTE parce qu ils en disaient plus que le code ne sait : « rien n a ete produit » est faux quand le plantage survient apres les ecritures. ETENDU AU JUMEAU : `point_du_jour.py` portait le meme `ignore` silencieux et reutilise desormais la fonction du canon plutot qu une seconde definition.
- **`VScode5:export-scelle`** (2026-09-01) : ACCEPTE + APPLIQUE sur DEUX volets sur trois (demande utilisateur « securise les fichiers de export », les trois options retenues). `export/` est un kit auto-portant que 5 depots installent chez eux — hooks executes au demarrage de session, installateur qui ecrit dans .claude/ et fusionne settings.json — et il n avait AUCUNE protection. (1) EDITION MANUELLE : nouveau hook `guard_export_genere.py`, cable en PreToolUse, qui REFUSE un commit embarquant export/ quand `--check` signale une derive. Il bloque la ou `warn_verif_before_commit` avertit, parce que la consequence est silencieuse ET differee — le travail ne disparait pas au commit mais a la regeneration suivante, quand plus personne ne fait le lien ; c est ce trou qui a laisse servir un agent-orchestrator de 120 lignes contre 467. Fail-open partout. (2) INTEGRITE : `MANIFESTE.json` porte desormais un sha256 par fichier (43/43) et `install_agentic.py` REFUSE un fichier dont l empreinte ne correspond pas. Le manifeste voyage avec le kit : le controle de destination pose plus tot ferme OU l on ecrit, celui-ci ferme QUOI. Retro-compatible : un kit ancien sans empreinte s installe encore, refuser transformerait une amelioration en panne de deploiement. 9 tests, les 4 qui pouvaient echouer vus ROUGES. (3) REVUE DE SECURITE DU CONTENU PUBLIE : dispatchee en sous-agent `bmad-revue` (opus), NON RENDUE a l heure ou ceci est ecrit — ce volet reste OUVERT.
- **`VScode5:refonte-site-ruptures-A-et-B`** (2026-09-01) : ARBITRE « B puis A » par l utilisateur, et R1 A CHANGE LE TRAVAIL. La proposition `docs/reflexions/approche-disruptive-wiki-2026-07-31.md` porte « rien n a ete applique » ; c etait son etat a la redaction et plus le notre. Verifie avant d ecrire : la RUPTURE B est appliquee depuis le 2026-07-31 — `point_du_jour.py` la porte explicitement dans son docstring, le dispositif vient a l utilisateur par le canal reellement utilise. La moitie « contenu » de la RUPTURE A l est aussi : le bloc « Ce qui a casse / Ce qui attend votre decision / Depuis le scan precedent » existe depuis juillet. NE RESTAIT QUE LA SECONDE MOITIE DE A, celle que la mesure condamne sans discussion : la salle demandait que « les 11 onglets se subordonnent » et que le reste devienne « une archive consultable, pas une facade a parcourir ». Mesure du 2026-09-01 contre la sienne du 2026-07-31 : onglets 11 -> 11, page 278 Ko -> 458 Ko, 26 230 mots -> 49 345. La page avait presque DOUBLE pendant qu on demandait qu elle se subordonne, et le bloc de reponse vivait DANS le panneau Pilotage, donc apres la barre d onglets et invisible depuis les dix autres. APPLIQUE : la reponse est remontee au niveau de la page, avant la navigation — elle est la premiere chose lue et reste lisible quel que soit l onglet. Subordonner n est pas supprimer : aucun onglet retire, ce serait une autre decision que personne n a arbitree. 5 tests, dont un qui verifie le GENERATEUR et pas seulement le rendu. UN TEST DE MA MAIN CORRIGE : j exigeais les trois lignes en permanence, or la troisieme est conditionnelle — entre deux scans consecutifs rien ne bouge, et une ligne qui dirait « rien n a bouge » serait du bruit. C etait le test qui affirmait faux, pas le code. RESTE NON FAIT ET NON ARBITRE : reduire le nombre d onglets, et la rupture C (retirer la console).
- **`flotte:warn_verif_before_commit`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (option « généraliser dans VSCode3 », arbitrée par l'utilisateur le 2026-09-02 ; l'option « le hub porte sa propre version générique » a été écartée parce qu'elle aurait forké la source). Le finding était ouvert depuis la revue de sécurité du 2026-09-01 et CLAUDE.md le portait en « conséquence non résolue ». DEUX DÉFAUTS, à ne pas confondre. (1) Le déclencheur était propre à VSCode3 — `_WATCHED_PREFIXES = ("docs/cadrage-ppt/",)` — alors que ce fichier est la SOURCE du kit publié (`export_agentic.GENERIQUE` pointe `~/Documents/VSCode3/.claude/hooks`) : installé ailleurs, le garde-fou pré-commit ne se déclenchait jamais sur du code applicatif. (2) Le message et le docstring parlaient de `app/**` et de `npm test`, héritage de VSCode1 jamais mis à jour le 2026-07-24 : quand le hook SE déclenchait, il envoyait lancer la MAUVAISE commande, les preuves acceptées étant `pytest`. Le défaut n'était donc pas « il ne surveille rien » mais « il surveille le répertoire d'un autre et décrit celui d'un troisième ». CORRECTIF : les trois listes sont lues dans un `.claude/warn_verif_before_commit.json` optionnel, par un chemin dérivé de `__file__` et non du `cwd` (un commit lancé depuis un sous-dossier ne doit pas faire manquer la configuration) ; repli générique champ par champ ; le message est COMPOSÉ à partir des valeurs réellement configurées. Le fail-open promis par le docstring est préservé, lecture de configuration comprise. VSCode3 garde exactement son comportement, restitué par sa propre configuration et verrouillé par un test. Son CLAUDE.md, qui disait d'éditer les constantes du hook — donc de reproduire le défaut — pointe désormais la configuration. 14 tests neufs dont 10 VUS ROUGES avant correction, suite complète de VSCode3 90 passés, commit scopé 56cdc04. Kit régénéré : ce qu'un dépôt tiers obtiendra désormais surveille `app/` et nomme ses propres valeurs.
- **`.claude/dispositif/package/install_agentic.py::_fusionner_settings`** (2026-09-02) : FINDING PÉRIMÉ À L'INSTRUCTION, refermé sans nouveau code (R1 : lire l'état réel avant d'écrire). Le finding, ouvert par la revue de sécurité du 2026-09-01, demandait de valider `isinstance(hooks[evenement], list)` pour que `_fusionner_settings` cesse de planter APRÈS la copie des 43 fichiers, sans rollback, sur un `settings.json` dont un événement de hook n'est pas une liste. Vérifié sur disque le 2026-09-02 avant d'agir : les gardes existent déjà (`install_agentic.py` lignes 108 à 142 — `isinstance(existant, dict)`, vérification de type par clé attendue, `isinstance(permissions.get("deny", []), list)`, et filtrage `isinstance` sur les groupes et les hooks). Le correctif avait été écrit la veille au soir et n'avait simplement jamais été commité ni tracé — c'est le commit 2e57112 qui le porte. Écrire une seconde fois ce qui existe déjà aurait produit un diff cosmétique et un faux sentiment de remédiation : le finding se solde par ce constat, pas par du code.
- **`export/orchestration/git_agents_inventory.py`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur du 2026-09-02). Corrigé À LA SOURCE (`.claude/orchestration/git_agents_inventory.py`) puis republié : `export/` est entièrement généré, y éditer quoi que ce soit se perd à la régénération suivante. DEUX DÉFAUTS dans l'outil qui répond « un agent équivalent a-t-il déjà existé ? » — donc dans l'outil d'escalade qui empêche de recréer un agent qui existait. (1) `core.quotepath` vaut `true` par défaut (vérifié : `git config --get core.quotepath` ne rend rien) : git échappe les octets ≥ 0x80, tout chemin accentué revient sous une forme qui ne correspond à aucun nom réel, et la recherche répond NON à tort. Corrigé par `-c core.quotepath=false` dans l'unique point d'entrée git du fichier — un test verrouille désormais qu'il n'y en ait qu'un. (2) Les en-têtes de commit étaient distingués par un `@` en tête, or un chemin peut légalement commencer par `@` : reproduit sur un vrai dépôt jetable avec un vrai `@robot/agents/special.md` supprimé, le parseur levait `ValueError: not enough values to unpack`. LE CHOIX DU REMPLAÇANT A ÉTÉ CORRIGÉ PAR L'EXÉCUTION, PAS PAR LA RELECTURE : le premier séparateur retenu, `\x1e` (Record Separator), est silencieusement traité comme une fin de ligne par `str.splitlines()` de Python et disparaît — aucun en-tête n'aurait plus jamais matché, et le défaut aurait été remplacé par un pire, muet. `\x1f` survit à `splitlines()`, et Windows interdit les caractères de contrôle dans les noms de fichier tandis que git les échappe toujours : la collision est impossible. 7 tests, vus ROUGES sur un vrai dépôt git jetable — jamais par monkeypatch.
- **`export/skills/pptx-framed-image/scripts/stock_images.py`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur du 2026-09-02), EN DEUX VOLETS dont le second n'était pas dans le finding. Corrigé à la source (`.claude/skills/pptx-framed-image/scripts/stock_images.py`) puis republié et propagé aux 5 copies de la flotte. VOLET 1, celui du finding : la garde posée le 2026-09-01 filtrait le SCHÉMA (elle avait fermé `file://`) mais pas la DESTINATION — une url http vers `127.0.0.1`, `169.254.169.254` (métadonnées cloud) ou `192.168.x.x` la traversait, alors que l'url vient d'Openverse, un AGRÉGATEUR de sources tierces, donc une donnée non contrôlée. L'hôte est désormais RÉSOLU puis refusé s'il est de bouclage, privé, link-local, réservé, multicast ou non spécifié — IPv4 mappée en IPv6 (`::ffff:127.0.0.1`) comprise, le piège qui contourne une garde naïve. Fail-closed assumé et documenté : un hôte qui ne résout pas est REFUSÉ, l'incertitude d'une garde de sécurité se tranche du côté du refus. VOLET 2, SIGNALÉ PAR L'AGENT LUI-MÊME ET RENVOYÉ POUR TRAITEMENT : `urllib.request.urlopen` suit les redirections 3xx tout seul, donc une url publique légitime qui répond `302 Location: http://127.0.0.1:8765/` traversait la garde intacte — la garde validait une adresse et le programme en visitait une autre. Un garde-fou qu'un 302 défait n'en est pas un, et c'est la famille de défaut la plus répétée de ce dépôt. Chaque saut est re-validé par un `HTTPRedirectHandler` dérivé, monté dans un opener DÉDIÉ (pas `install_opener` : le comportement global d'urllib n'est pas modifié pour le reste du process), et la limite de sauts héritée n'est pas supprimée — elle protège d'autre chose, une boucle infinie. 24 tests, vus ROUGES avant chaque volet, dont une couche qui traverse la VRAIE mécanique d'urllib et ne remplace que `HTTPConnection`. RESTE OUVERT ET DIT, non traité sciemment : le DNS rebinding (TOCTOU entre la résolution de la garde et celle de la connexion — fermer cela demande de figer la résolution jusqu'à la connexion, chantier à part) et les plages NAT64/CGNAT, couvertes partiellement par `is_private`/`is_reserved`.
- **`.claude/dispositif/sync_dispositif.py`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur du 2026-09-02). Le refus de synchronisation imprimait « le script a été modifié ici », un message qui désigne LA CIBLE comme fautive, alors que deux causes distinctes produisent la même différence : soit la copie de la cible a réellement divergé, soit le CANON a avancé au hub depuis qu'elle l'a reçu — auquel cas la cible est intacte et seulement en retard, et le message l'envoie chercher une modification locale qui n'existe pas. CORRECTIF, en réutilisant le mécanisme de provenance que `propager_socle.py` porte déjà plutôt qu'en inventant un second (deux définitions d'une même chose finissent par diverger — c'est un constat déjà payé ici le 2026-09-01) : la copie déployée porte la révision du canon dont elle est issue, et le refus compare son corps au canon À CETTE RÉVISION, récupéré par `git show`. Trois messages distincts en sortie : « la cible a divergé », « le canon a avancé », et un troisième délibérément NON accusatoire pour les copies antérieures au mécanisme, qui dit « impossible de déterminer » plutôt que d'affirmer une cause. Le comportement d'écriture est inchangé — le refus reste le défaut, `--accepter-derive` reste le seul levier : le finding demandait de cesser de nommer le mauvais coupable, pas de changer ce qui est écrasé. Les deux `subprocess.run` ajoutés portent `encoding="utf-8", errors="replace"` dès l'écriture, et la porte AST du dépôt le vérifie. 7 tests, vus ROUGES, sur un vrai dépôt jetable à deux révisions successives du canon.
- **`VScode5:raccord-bmad`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (demande utilisateur du 2026-09-02 : « sur le site il est indiqué que des skills bmad ne sont jamais utilisées, vérifie cette info et fais en sorte qu'elles soient bien utilisées lorsque demandées au niveau de ce projet, que ce raccord soit bien en place dans le répertoire export dans la structure agentic »). L'INFORMATION DU SITE EST EXACTE, et la mesure est pire que ce qu'elle affiche : étage 1 cumulatif, sur 46 skills BMAD installées, 2 SEULEMENT ont jamais été invoquées — `bmad-party-mode` (7 fois, par les salles) et `bmad-customize` (1 fois, en direct), TOUTES DEUX SANS PORTEUR ; et le sous-agent porteur `bmad-revue`, dont le mandat dit « invoque réellement les skills bmad-* » et qui déclare déjà un champ `SKILL INVOQUÉE` dans son contrat de sortie, a tourné 5 fois sans en charger une seule. LE DÉFAUT N'ÉTAIT PAS OÙ ON L'ATTENDAIT (R1) : les skills ne manquent nulle part — hub 46, VSCode 71, VSCode1 46, VSCode3 46, VSCode4 46, VSCode2 39. Ce qui manquait, c'est que le brief envoyé au porteur — seul document qu'il reçoit, son contexte étant vierge — ne portait pas le NOM de la skill à charger. TROIS GESTES : (1) le § 2 quinquies de la skill d'orchestration impose désormais que le brief porte le nom exact, fait de l'invocation un contrat de sortie vérifiable par le compteur de l'étage 1, et traite le contrat non rempli comme toute étape ratée — une relance ciblée puis escalade, jamais réécrire le rapport à la main ; (2) les mandats de `bmad-revue` et `bmad-recherche` transforment `SKILL INVOQUÉE` en PREUVE, avec `aucune` comme réponse honnête et explicitement préférable à un nom emprunté ; (3) l'installateur du kit signale à l'installation les skills routées que la cible n'a pas. ET CE TROISIÈME GESTE A CRIÉ SUR UNE CIBLE SAINE AVANT D'ÊTRE JUSTE : lancé sur la flotte réelle, il réclamait 9 skills à VSCode2 et avertissait même VSCode1 qui possède les 46, parce qu'il ramassait la colonne 3 du tableau (le SOUS-AGENT porteur, qui vit dans `.claude/agents/` et non `.claude/skills/`) et la prose des skills DÉPRÉCIÉES par BMAD, qu'il envoyait donc installer. Corrigé à la colonne 2 des vraies rangées de tableau, et re-vérifié sur l'oracle réel : VSCode2 3 manquantes exactes (`bmad-spec`, `bmad-quick-dev`, `bmad-qa-generate-e2e-tests`), les 4 cibles complètes muettes. Le test qui aurait dû l'attraper ne cherchait qu'un nom dans la sortie et restait vert : son assertion a été élargie à l'absence de tout avertissement. 11 tests, vus ROUGES. Socle propagé aux 5 dépôts, chapitres locaux préservés, commits scopés.
- **`VScode5:tokens-fenetre`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (option « publier avec la fenêtre affichée », arbitrée le 2026-09-02 ; l'option « construire une série durable » a été écartée comme chantier à part, et « restaurer juillet » comme le pire des trois). Point de départ : demande utilisateur « les informations du site ne semblent pas à jour ». Exact — l'onglet Tokens datait du 2026-07-31 15:17, soit 33 jours, parce que le scan régénérait tout SAUF lui. Le plus instructif est que la page AFFICHAIT déjà son propre diagnostic : son axe « Mesurer en continu, pas quand on y pense » disait mot pour mot que le compte « n'existe que si quelqu'un lance mesure_tokens.py à la main ». Un diagnostic publié un mois sans que rien ne l'exécute. ET LE RAFRAÎCHIR NAÏVEMENT AURAIT MENTI : la mesure agrège le cache ÉPHÉMÈRE des transcripts, que Claude Code purge — 124 fichiers / 8 jours le 2026-07-31, 10 fichiers / 4 jours le 2026-09-02, et le facturable tombe de 81 912 714 à 22 834 824, soit -72 % sans qu'aucune consommation ait baissé. La légende disait « sur tout l'historique disponible » : vrai au mot près, trompeur en pratique, puisque « disponible » rétrécit. DEUX GESTES : la page affiche désormais les jours couverts ET un avertissement explicite que le total n'est pas cumulatif, avec les deux relevés en regard ; et la mesure suit la cadence du scan, fail-open (le scan tourne dans un hook SessionStart). DÉFAUT VOISIN TROUVÉ EN CORRIGEANT : `mesure_tokens.py` écrivait à un chemin EN DUR alors que les trois autres journaux du dispositif sont redirigeables par variable d'environnement — un test qui l'exerçait écrasait la mesure de PRODUCTION, exactement la famille du défaut `jobs.jsonl`. Aligné sur la convention. 7 tests vus ROUGES.
- **`VScode5:findings-masques`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (option « les deux », arbitrée le 2026-09-02 : comparer les dates ET signaler ce qui était masqué). Point de départ : demande utilisateur « beaucoup de projets sont notés critiques, est-ce normal ? ». La réponse s'est retournée — le tableau ne surévaluait pas les alertes, il en CACHAIT. `scan_projets.py` écartait un finding dès que sa cible apparaissait dans l'historique des arbitrages, sans regarder la catégorie, ni la date, ni le drapeau `re_challenge` ; `diag_date` était lu deux lignes plus haut et jamais utilisé. Un finding dont la cible avait été arbitrée UNE fois devenait donc invisible pour toujours, et plus un sujet récidivait, plus sûrement ses nouveaux constats disparaissaient — l'inverse de ce qu'on attend d'un tableau de bord. Mesuré : 3 findings invisibles sur la flotte, dont un p5 — `revue-increment` chez VSCode2, éteint par 10 arbitrages datant tous de juillet alors que le constat était du 2026-09-01. ON N'A PAS ÉCRIT UNE TROISIÈME VERSION DE LA RÈGLE : le canon la tenait déjà juste depuis le 2026-07-28 — `finding_arbitre()` compare cible ET couverture de catégorie et n'oppose un arbitrage antérieur à un constat re-challengé que s'il est du jour même ou postérieur ; `diagnostic_masques()` rend visibles les écartés, « le filtrage était silencieux ». Les deux moitiés existaient, et le hub s'en était écrit une copie naïve à côté sans le savoir — le motif que ce dépôt paie en boucle. VÉRIFIÉ AVANT DE CÂBLER : appliquée aux 3 masqués, la règle du canon en rouvre 2 (ceux qui portent `re_challenge: True`) et laisse le 3ᵉ fermé — elle ne rouvre pas tout, elle rouvre ce que le superviseur avait explicitement re-challengé. Les écartés sont désormais rendus, barrés, hors décompte d'alerte. Fail-open : canon injoignable → aucun filtre, on montre tout ; un tableau qui montre trop se corrige en le lisant, un tableau qui cache ne se corrige pas. 7 tests vus ROUGES. Effet mesuré : 4 projets critiques → 2, et les 2 constats de VSCode2 jusque-là invisibles sont affichés.
- **`hooks:guard_salle_skills`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (arbitrage utilisateur du 2026-09-02, « point 5 à reprendre dans les sujets à déployer pour les projets de maintenant et ceux de demain ») : le hook guard_salle_skills.py entre dans le kit agentic — export_agentic.MANIFESTE + entrée PreToolUse/Skill du gabarit settings de l'installateur (kit 46/46 puis 48/48 avec les deux conceptions). Les projets futurs le reçoivent à l'installation ; les projets actuels à la propagation, qui est en STANDBY (cf. flotte:propagation-socle-standby). La revue de commit du même jour avait relevé que son docstring l'annonçait « publié » alors qu'il n'y était pas.
- **`VSCode2:export-conceptions`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (« reprend les éléments partiels de VSCode2 ») : les deux documents de conception (agent-orchestrator 0b414f9 du 2026-07-20, agent-supervisor f6504bb du 2026-07-29), sans équivalent vivant au hub, repris PARTIELLEMENT sous docs/reflexions/conception-agent-{orchestrator,supervisor}.md — rationale et décisions avec leur statut réel au hub, journal daté de VSCode2 écarté. FORMAT.md:29 (« §5 de la conception ») et le docstring de write_diagnostic.py pointent enfin un fichier qui existe. Les deux docs sont ajoutés au kit pour que ces renvois ne soient pas des liens morts chez les cibles. Rien touché chez VSCode2 (suppression indexée en cours, le contenu reste dans son historique git).
- **`VSCode1:export-docs-ppt`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (« reprend les éléments de VSCode1 ») : reprise PAR EXTRAITS, jamais par fichiers entiers — fiche du template OCTO partagé (md5 ef9f8c805133… identique chez VSCode1/2/3) dans deck-design-library/references/template-octo.md (au kit), recette de test cadre/image dans pptx-framed-image, seuils WCAG 4.5:1 / 3:1 et plancher 10,5 pt dans les skills globales pptx-verify / restitution-deck-design / pptx-deck (hors dépôt), cache de prompt dans le § tokens de CLAUDE.md. Les 5 docs restent chez VSCode1 (aucun orphelin, aucun supprimable) — dit à leur session. La section RTK d'optimisation-tokens.md est un historique mort, non repris.
- **`flotte:propagation-socle-standby`** (2026-09-02) : STANDBY (arbitrage utilisateur du 2026-09-02, « en standby la propagation vers les projets ») : propager_socle --dry-run mesurait 5 cibles à la provenance 2e57112, +23 lignes chacune, chapitre local préservé, 0 perte, aucun fichier localement touché — mais les 5 dépôts avaient une session Claude active le jour même. Conséquence assumée : les 2 tests de test_check_flotte (« socle à jour partout ») RESTENT ROUGES tant que le standby dure ; c'est l'alarme de dérive de flotte, pas un défaut à masquer. VSCode et VSCode1 sont en outre au TODO (finding flotte:VSCode,VSCode1). À lever par « propage » quand les cibles sont au repos.
- **`VScode5:seuil-qualification-non-mesurable`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (option A, arbitrage utilisateur du 2026-09-02 : « traite P1, P2, P7 et la rétrospective », sur le backlog priorisé du même jour). Le seuil « orchestrer ou exécuter directement » promettait depuis juillet une calibration sur données ; elle était impossible par construction — 106 runs, 106 `orchestre`, 0 `direct-signale`, parce que la méthode dit qu'une exécution directe ne se journalise pas. Le numérateur existait dans runs.jsonl, le dénominateur nulle part. APPLIQUÉ : `orchestrator_gate.py` — seul point du dispositif qui voit CHAQUE prompt — écrit une ligne par demande dans `.claude/orchestration/prompts.jsonl` : horodatage, booléen « commande slash », longueur en caractères, et RIEN DU TEXTE (contenu client ; la longueur suffit à séparer une micro-tâche d'un chantier). Chemin dérivé de `__file__` et non du `cwd` (leçon warn_verif_before_commit du 2026-09-02), fail-open intégral : un journal qui ne peut pas s'écrire ne coûte jamais une demande. La règle « une exécution directe ne fait pas un run » reste intacte — le hook COMPTE, il ne classe pas un prompt en « aurait dû être orchestré ». ÉCART RECONNU ET NON MAQUILLÉ : le code a été écrit AVANT cet arbitrage, ce que R4 interdit ; il est ratifié ici, pas légitimé rétroactivement. CORRECTION FAITE À LA RATIFICATION : tests/test_seuil_qualification.py comparait `n_car` à 34 pour un prompt de 33 caractères — le test tombait rouge en accusant le hook d'un défaut qui était le sien ; la longueur se dérive désormais de la demande envoyée. Suite du fichier : 11 passés. LIMITE À NE PAS OUBLIER : le journal démarre le 2026-09-02, il ne dit encore rien du passé — le ratio ne sera lisible qu'après une fenêtre d'usage réelle, et l'affirmer plus tôt serait le même défaut que celui qu'on corrige.
- **`VScode5:skills-pptx-globales-non-versionnees`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (option A, arbitrage utilisateur du 2026-09-02 : « traite P1, P2, P7 et la rétrospective »). Les trois skills dont les projets de deck dépendent — pptx-deck, pptx-verify, restitution-deck-design — vivaient dans le répertoire global `~/.claude/skills/`, hors de tout dépôt, hors du kit, donc ni versionnées, ni revues, ni propageables. APPLIQUÉ : rapatriées dans `.claude/skills/` du hub et publiées dans `export/skills/` + MANIFESTE, comme les 4 skills PPT déjà au kit. Kit re-vérifié après régénération : 58/58 à jour, 0 dérive. CE QUI N'EST PAS TRANCHÉ ICI, ET QUI RESTE OUVERT : les copies globales existent TOUJOURS dans `~/.claude/skills/`. Les supprimer est irréversible et retirerait ces skills à tout projet où le kit n'est pas encore installé — or la propagation du socle est en standby (arbitrage flotte:propagation-socle-standby du même jour). Le doublon est donc ASSUMÉ et TEMPORAIRE : la bascule global -> projet ne se solde qu'une fois le kit posé chez les cibles, et la suppression des trois répertoires globaux sera une décision utilisateur à part entière, jamais un effet de bord de cette entrée. ÉCART RECONNU : comme pour seuil-qualification, le rapatriement a précédé l'arbitrage.
- **`VScode5:portee-multi-projets-jamais-arbitree`** (2026-09-02) : ACCEPTÉ + APPLIQUÉ (arbitrage rétrospectif, aucune décision à prendre : la recommandation elle-même dit « rien à changer dans le code, la décision est tranchée dans les faits et elle marche » — proposée dans le backlog priorisé du 2026-09-02, traitée dans le même run car sans arbitrage réel à attendre). La conception de juillet (VSCode2 §10.4) posait le choix entre un superviseur mono-projet et une skill globale `~/.claude/skills/` ; le réel est une TROISIÈME voie, un dépôt hub dédié qui agit sur d'autres dépôts, sans qu'aucune entrée d'arbitrages.json ne la fonde — alors qu'elle porte à elle seule R1, R2 et R3 de CLAUDE.md. Ce que le dépôt dédié permet et que les deux autres voies interdisaient : (1) agir sur un dépôt TIERS avec un commit scopé au périmètre (R2) — une skill globale ou mono-projet n'a pas de notion de « périmètre d'un autre dépôt » à respecter ; (2) adapter chaque correctif au canal réel de la cible (R3) — COMOP chez VSCode, python-pptx chez VSCode2, npm chez VSCode1 — ce qu'une skill unique, portée par UN SEUL projet, ne peut pas arbitrer depuis l'intérieur de ce projet ; (3) porter un journal (`runs.jsonl`) et des arbitrages (`arbitrages.json`) qui SURVIVENT à la session et à la mémoire d'un poste — une skill globale n'a ni l'un ni l'autre, une skill mono-projet les aurait confondus avec ceux du projet qu'elle supervise. CE QUE CETTE ENTRÉE NE FAIT PAS : elle ne change rien au dispositif — elle documente un choix déjà en vigueur depuis juillet, pour que la question ne se repose pas sans qu'on sache pourquoi le dépôt dédié a été retenu.
- **`VScode5:openhub-code-herite`** (2026-09-03) : ACCEPTÉ + APPLIQUÉ CÔTÉ HUB, ARCHIVAGE VSCode2 DIFFÉRÉ (arbitrage utilisateur du 2026-09-03, « traite tous les sujets identifiés dans arbitrage »). Tranché SUR UN FAIT et non sur une question posée à un humain, comme R6 l'impose quand la vérification est exécutable à 0 token : requête directe sur `data/app.db` de VSCode2 (modifié le jour même par l'usage réel du produit — missions, interviews — donc pas un projet à l'arrêt) : la table `agent_results` reste à **0 ligne**, même sous activité réelle. L'instrumentation n'a jamais rien produit nulle part, confirmant la branche « non » de la proposition. APPLIQUÉ : `openhub_stats()`, la constante `OPENHUB_DB` et le paramètre `openhub` des trois fonctions de rendu (markdown + HTML) retirés du canon — un seul endroit, propagé au hub lui-même par sync_dispositif. 0 test ne couvrait cette instrumentation (mesuré avant retrait), donc retrait sans risque de régression ; suite complète 1028 passed après retrait. DIFFÉRÉ : « archiver OPENHUB.md côté VSCode2 » — ce dépôt porte une session Claude interactive active et 26 fichiers non commités d'un tiers au moment de cet arbitrage (R2 : ne pas embarquer ou perturber du travail en cours qui n'est pas le nôtre). À reprendre via le playbook evolution-flotte quand VSCode2 sera au repos — même geste que le standby déjà posé sur la propagation du socle.
- **`veille:borne-murale-sous-agent-non-convergent`** (2026-09-03) : ADOPTE + APPLIQUE (arbitrage utilisateur du 2026-09-03, « met en place les 3 points proposes contre les blocages/hallucinations »). Constat source : un sous-agent audit-technique est reste running 4h+ contre 8-17 min pour 4 taches comparables, arrete sans resultat (TaskStop). Aucune duree n'etait calculable (SubagentStop capte la fin, jamais le couple lancement/fin). APPLIQUE : `log_usage.py::_duree_appariee()` calcule un `duree_s` sur un SubagentStop UNIQUEMENT quand un seul lancement Agent de la session reste ouvert — deux lancements concurrents (le cas courant de ce dispatcher, fan-out ≤4) ne produisent volontairement AUCUNE duree plutot qu'une duree devinee (meme principe que _echec_avere : une mesure fausse est pire qu'aucune mesure). 4 tests neufs, dont un qui verifie l'ambiguite du fan-out concurrent. `agent-supervisor/SKILL.md` : categorie `non-convergence` etendue a un second signal (sous-agent d'arriere-plan sans notification ≥3-5x la p95 des runs comparables), distinct du premier (livrable rejete a repetition). `agent-orchestrator/SKILL.md` §2 ter : regle operationnelle documentee (TaskOutput non bloquant puis TaskStop plutot qu'attendre indefiniment, jamais maxTurns frontmatter comme filet, jamais fabriquer un resultat). Suite complete 1046 passed, kit regenere 58/58.
- **`veille:depot-au-repos-avant-dispatch`** (2026-09-03) : ADOPTE + APPLIQUE (arbitrage utilisateur du 2026-09-03, meme run). Le depot cible du sous-agent bloque portait une session interactive active en continu pendant les 4h+ — contention plausible, jamais verifiee avant dispatch. APPLIQUE (documentation, garde-fou maison assume comme tel — aucune source publique ne le prescrit) : `audit-technique/SKILL.md` etape 1 (Cadrer) et `evolution-flotte.md` etape cadrage-reel exigent desormais deux releves `git status --porcelain` espaces du depot cible avant tout dispatch de sous-agent de lecture/audit — un ecart signale une session tierce active, cause probable de non-convergence par contention plutot qu'une panne de l'agent.
- **`veille:non-evalue-et-relecture-audit-technique`** (2026-09-03) : ADOPTE + APPLIQUE (arbitrage utilisateur du 2026-09-03, meme run). Contraste avec le guide Anthropic anti-hallucination (« allow Claude to say I don't know », « verify with citations, retract if unsupported ») : le grounding par citation etait deja cable (audit-technique/SKILL.md, « pas de constat sans fichier:ligne »), mais deux manques precis subsistaient. APPLIQUE : (1) niveau `non_evalue` ajoute aux 3 niveaux imposes par dimension (ok/moyen/critique) — une dimension non couverte faute de temps/acces peut le dire plutot que forcer un niveau ; `PASTILLE` de scripts/scan_projets.py etendue en consequence (sinon KeyError au premier audit l'utilisant, verifie avant d'ecrire la doc seule). (2) etape 3bis « relecture » ajoutee entre Qualifier et Ecrire : tout finding sans fichier:ligne est retire avant l'ecriture du JSON — la regle se verifie desormais en re-scannant, pas seulement en consigne au moment de rediger.
- **`flotte:deploiement-garde-fous-non-convergence`** (2026-09-03) : ACCEPTE + APPLIQUE (demande utilisateur du 2026-09-03, "ajoute le systeme de traitement des hallucinations des agents/sous-agents au hub et deploie le a l ensemble des projets") : les 3 garde-fous adoptes en veille le meme jour (veille:borne-murale-sous-agent-non-convergent, veille:depot-au-repos-avant-dispatch, veille:non-evalue-et-relecture-audit-technique) propages aux 5 projets de la flotte, chacun apres double releve git status --porcelain confirmant le depot au repos (VSCode1 et VSCode3 avaient des sessions tierces actives plus tot dans la seance, retraites en dernier une fois confirmes au repos). Fichiers propages par cible : agent-orchestrator/SKILL.md (via propager_socle.py, chapitre local preserve), audit-technique/SKILL.md, log_usage.py, agent-supervisor/SKILL.md, evolution-flotte.md (diff-verifies avant ecrasement : aucune personnalisation locale trouvee sur aucune cible). Commits : VSCode 221e390, VSCode4 31cce62, VSCode2 8d61357, VSCode1 2186f45, VSCode3 254844f. Lisse partiellement la standby flotte:propagation-socle-standby (2026-09-02) pour ces 5 fichiers precis, mais NE REPOND PAS au finding diagnostic flotte:VSCode,VSCode1 (realignement complet du kit via export_agentic.py --check-flotte, perimetre bien plus large : 15-26 fichiers differents/absents restants selon la cible) — ce finding reste ouvert.

## Diagnostic qualitatif (étage 2 — `agent-supervisor`)

_Diagnostic à jour._

1. **Chantier a planifier (demande utilisateur du 2026-09-02) : realigner la suite agentic de VSCode et VSCode1 sur le kit du hub** — Ne pas propager tant qu'une session tierce travaille dans la cible ; a lancer des que VSCode et VSCode1 sont au repos (git status propre), en une seance dediee par cible. Corollaire du 2026-09-03 : un écart mesuré par --check-flotte n'est pas automatiquement une anomalie de PROPAGATION — il peut être une décision locale jamais remontée. Les deux causes ne se corrigent pas au même endroit (l'une par propagation, l'autre par un arbitrage de canon) et le mélange coûte une relecture inutile, comme le finding voisin `.claude/dispositif/sync_dispositif.py` le documente déjà pour un autre outil de mesure. · **Proposition** : Instancier le playbook evolution-flotte par cible : (1) `--check-flotte` relu ligne a ligne, un ecart n'est pas une derive (R1/R3) ; (2) `propager_socle.py --dry-run --projet <cible>` puis `--appliquer` sans --accepter-pertes tant que la liste des lignes locales perdues n'a pas ete LUE ; (3) suites de tests de la cible rejouees ; (4) commit scope au kit ; (5) `--check-flotte` re-mesure et journal. Le meme ecart de 24 fichiers existe chez VSCode2/3/4 (sessions actives aussi) : a traiter dans la foulee, hors de ce TODO. Ajout du 2026-09-03, propre à VSCode1 et hors périmètre de la propagation du kit : la décision locale du 2026-07-28 de retirer `revue-design-parallele.md` doit être explicitement arbitrée au niveau du hub — soit retirée du canon fleet-wide (si le pattern fan-out d'export-ppt-verifie est jugé généralisable), soit actée comme un écart VSCode1 assumé et documenté dans son propre catalogue plutôt que re-signalée à chaque propagation comme une résurrection accidentelle. Sans cet arbitrage, le même 'faux positif' se representera à la prochaine synchro de VSCode1.

_9 constat(s) de ce diagnostic écarté(s) par un arbitrage — pour en rouvrir un, demander au superviseur un `re_challenge` avec des données nouvelles :_

- ~~Le kit publie embarque les chemins surveilles d un AUTRE projet : ailleurs, le garde-fou pre-commit est un no-op muet~~ (`flotte:warn_verif_before_commit`)
- ~~Un settings.json dont un evenement de hook n est pas une liste fait planter l installateur APRES la copie des 43 fichiers, sans rollback~~ (`.claude/dispositif/package/install_agentic.py::_fusionner_settings`)
- ~~L outil qui repond « un agent equivalent a-t-il deja existe ? » repond NON a tort sur tout chemin accentue, et plante sur un chemin commencant par arobase~~ (`export/orchestration/git_agents_inventory.py`)
- ~~La garde SSRF filtre le SCHEMA mais pas la DESTINATION : une url http vers une adresse interne reste telechargee~~ (`export/skills/pptx-framed-image/scripts/stock_images.py`)
- ~~Le refus de synchronisation accuse la cible alors que c est le canon qui a bouge~~ (`.claude/dispositif/sync_dispositif.py`)
- ~~Trois skills dont les projets de deck dependent vivent dans un repertoire NON VERSIONNE, hors de tout depot et hors du kit~~ (`VScode5:skills-pptx-globales-non-versionnees`)
- ~~Le code de couverture OpenHub est inerte au hub mais VIVANT dans le canon propage aux 5 cibles, et n a jamais produit une seule ligne nulle part~~ (`VScode5:openhub-code-herite`)
- ~~Le ratio qui devait calibrer le seuil « orchestrer ou executer directement » est non mesurable par construction : 106 runs, 106 orchestre, 0 direct-signale~~ (`VScode5:seuil-qualification-non-mesurable`)
- ~~La decision la plus structurante du dispositif — un depot hub dedie plutot qu une skill locale ou globale — n a aucune trace de deliberation~~ (`VScode5:portee-multi-projets-jamais-arbitree`)

## Seuil de qualification — la mesure

Depuis le 2026-09-02 : **9** demande(s) vue(s) hors commande slash (+ 5 slash), **6** run(s) orchestré(s) journalisé(s) sur la même fenêtre — soit **67 %** des demandes orchestrées.
_Ce chiffre ne dit pas ce qui AURAIT dû être orchestré : le hook compte, il ne juge pas. Il donne le dénominateur qui manquait pour arbitrer le seuil sur données plutôt que sur habitude._

---

_Étage O-C (croisement modèle × tâche × reprises, exploitation de `runs.jsonl`) : voir `.claude/orchestration/routing-hints.json`, régénéré à chaque session._
